{
  "project": {"name": "ts-lint-failure", "languages": ["typescript"]},
  "contracts": {"output_dir": "qa/contracts"},
  "fast": {
    "output_dir": ".qa-z/runs",
    "fail_on_missing_tool": true,
    "checks": [
      {
        "id": "ts_lint",
        "enabled": true,
        "run": ["python", ".qa-z-benchmark/fast_check.py", "fail", "src/invoice.ts:1:7 error no-unused-vars"],
        "kind": "lint"
      }
    ]
  },
  "deep": {"checks": []}
}
