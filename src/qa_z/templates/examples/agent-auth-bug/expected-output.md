# Expected Output

The demo should produce a `do_not_merge` guard verdict because the included bad auth change fails deterministic tests.

Key artifacts:

- `.qa-z/runs/latest/guard/verdict.json`
- `.qa-z/runs/latest/review/review.md`
- `.qa-z/runs/latest/repair/codex.md`
