[mypy]
# Keep the cache outside the workspace on Windows. The compiled mypy 1.20.0
# build crashes with access violation 3221225477 when it uses a workspace-local
# cache path under F:\JustTyping.
cache_dir = $TEMP/qa-z-mypy-cache
