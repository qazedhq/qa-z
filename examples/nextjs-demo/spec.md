# Spec

Keep invoice visibility deterministic:

- finance users can view every invoice;
- support users can view only assigned invoices;
- unrelated users need explicit access;
- the page copy must come from covered TypeScript logic.
