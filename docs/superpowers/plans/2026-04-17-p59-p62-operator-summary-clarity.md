# P59-P62 Operator Summary Clarity Plan

1. Add failing tests for plain-text backlog focus, autonomy status open-session detail, and additive backlog-top-item fields.
2. Extend compact backlog-top-item shaping with title, recommendation, and evidence summary.
3. Replace the flat human backlog printout with an open-first summary that collapses closed history to a count.
4. Update README, schema docs, and current-truth tests, then run focused and full validation plus real-repo `backlog` and `autonomy status` checks.
