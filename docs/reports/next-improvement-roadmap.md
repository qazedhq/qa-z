# QA-Z Next Improvement Roadmap

Date: 2026-04-28
Branch context: `codex/qa-z-bootstrap`

## Purpose

This roadmap tracks the highest-value follow-up work after the integrated
executor-history, dry-run provenance, mixed-surface realism, and autonomy-control
passes already present in the worktree. It is grounded in repository evidence,
not in hypothetical future architecture.

It assumes the current cleanup reasoning remains in force:

- use `docs/reports/worktree-commit-plan.md` as the commit split order
- keep generated runtime artifacts out of source commits
- do not start live executor work until the current baseline is commitable and
  validated

Status update:

- the external executor return contract and deterministic return path are landed
- executed mixed-surface realism coverage is landed and now in breadth-expansion mode
- session-local multi-result history plus the live-free safety dry-run are landed
- dry-run provenance now stays visible across repair-session, publish, benchmark, and self-inspection surfaces
- generated versus frozen evidence policy is now explicit in `docs/generated-vs-frozen-evidence-policy.md`
- worktree helper and alpha-release evidence now distinguish local-only runtime artifacts from local-by-default benchmark evidence instead of treating generated output as one opaque bucket
- runtime artifact cleanup now mirrors that generated policy split by reusing the strict worktree helper's generated-policy buckets, so `--apply` clears all discovered local-only roots while benchmark result roots stay review-only until an operator makes the local-versus-frozen decision explicitly
- the latest compact strict worktree helper refresh after cleanup now reports `generated_artifact_count=0`, `generated_local_only_count=0`, `generated_local_by_default_count=0`, `cross_cutting_count=12`, `cross_cutting_group_count=5`, `shared_patch_add_count=16`, `unassigned_source_path_count=0`, and `multi_batch_path_count=0`; the matching include-ignored policy snapshot reports `generated_artifact_count=7`, `generated_local_only_count=0`, and `generated_local_by_default_count=7`
- deferred cleanup, commit-isolation, and integration report-only evidence is not enough to reopen those candidates; self-inspection now requires live git or runtime artifact evidence before report wording can seed that work
- loop health now has explicit `loop_health` summaries and repeated-blocked stop reasons
- mixed-surface benchmark breadth now includes a fourth executed fast-plus-deep slice that keeps repair handoff fast-only under non-blocking multi-source deep scan warnings
- the latest published-alpha verification refresh is `1212 passed` for pytest and `54/54 fixtures, overall_rate 1.0` for the benchmark corpus, with `mypy src tests` passing on `507 source files`, `ruff check src tests scripts` passing, and `ruff format --check src tests scripts` reporting `519 files already formatted`
- Priority 4 report/template/example sync now has a closed first pass, including README repo-map honesty, authoritative public example wording, and workflow-template non-goal comments for executor-result ingest and autonomous repair.
- Priority 5 executor operator diagnostics now includes the committed non-blocked `validation_conflict + repeated_rejected_attempts` mixed-history slice across logic, benchmark, publish, and continuity surfaces.
- live repository signals now keep dirty `benchmarks/results-*` snapshot roots in `benchmark_result_paths` and `dirty_benchmark_result_count` instead of collapsing them into `runtime_artifact_paths`
- alpha release preflight now distinguishes tracked local-only runtime artifacts from tracked local-by-default benchmark evidence and carries the split through compact gate evidence, schema, and release docs
- cleanup and workflow prepared actions now keep loop-local self-inspection continuity through `context_paths`, and release continuity assertions live in their own split current-truth pack so that contract stays pinned without reopening architecture-budget drift
- local alpha release gate passed for the published baseline, remote `main` now points at `6ed3a205443498f4bd063a97106fd9d3a05b99a0`, annotated tag `v0.9.8-alpha` was pushed, and GitHub prerelease `QA-Z v0.9.8-alpha` is published at `https://github.com/qazedhq/qa-z/releases/tag/v0.9.8-alpha`
- GitHub Actions run `25050794900` succeeded for jobs `test` and `qa-z`
- package registry publish remains intentionally out of scope for this GitHub prerelease step
- Immediate next focus: keep post-publish evidence surfaces aligned, then decide separately whether and when PyPI/TestPyPI publishing should get its own gate.

## Execution Progress

- 2026-04-28: Published `QA-Z v0.9.8-alpha` as a GitHub prerelease after importing the repository to `qazedhq/qa-z`, pushing annotated tag `v0.9.8-alpha`, and confirming GitHub Actions run `25050794900` succeeded for jobs `test` and `qa-z`. The published baseline is commit `6ed3a205443498f4bd063a97106fd9d3a05b99a0`; local final evidence recorded `1212 passed`, `54/54 fixtures, overall_rate 1.0`, `507 source files`, `519 files already formatted`, alpha release gate `29/29`, with-dependency artifact smoke, and artifact contents scan `0` forbidden files. No PyPI, TestPyPI, or package-registry publish was performed.
- 2026-04-24: L20 closed final local verification for this run. `python -m pytest` passed with `1184 passed`, `python -m mypy src tests` passed on `500 source files`, `python -m ruff check .` passed, `python -m ruff format --check .` reported `562 files already formatted`, and `python -m qa_z benchmark --json` stayed `54/54 fixtures, overall_rate 1.0`. The final strict worktree snapshot still has no generated visible source artifacts and no ownership drift, but correctly remains `attention_required` for the five cross-cutting patch-add groups. Final cleanup left only the seven review-only local-by-default benchmark evidence roots in the include-ignored policy snapshot: `generated_artifact_count=7`, `generated_local_only_count=0`, and `generated_local_by_default_count=7`. Fresh direct remote preflight against `https://github.com/qazedhq/qa-z.git` still failed with `remote_blocker=repository_missing` and `remote_readiness=needs_repository_bootstrap`, so publish remains parked on external repository bootstrap rather than a local QA-Z gate.
- 2026-04-24: L19 refreshed the post-gate cleanup and status baseline after the local alpha gate passed with the expanded current-truth split pack. `python scripts/runtime_artifact_cleanup.py --apply --json` deleted 17 local-only runtime artifact roots, left 7 local-by-default benchmark result roots in `review_local_by_default`, and reported `skipped_tracked=0`; `python -m qa_z self-inspect --json` then confirmed `runtime_artifact_count=0`, `release_evidence_count=0`, and the same live dirty-area split for the still-unsplit worktree; `python scripts/worktree_commit_plan.py --summary-only --json --fail-on-generated --fail-on-cross-cutting --output %TEMP%\qa-z-worktree-plan-l19-compact.json` stayed `attention_required` only for `cross_cutting_paths_present`, with `generated_artifact_count=0`, `generated_local_only_count=0`, `generated_local_by_default_count=0`, `cross_cutting_count=12`, `cross_cutting_group_count=5`, `shared_patch_add_count=16`, and `unassigned_source_path_count=0`; and the include-ignored companion kept policy-managed ignored output reviewable with `generated_artifact_count=15`, `generated_local_only_count=8`, and `generated_local_by_default_count=7`. The next repo-local package is commit-batch patch-add ownership for the five cross-cutting groups, while remote publish stays parked until `qazedhq/qa-z` is created/exposed or the expected target is deliberately realigned.
- 2026-04-24: L12 refreshed the generated-policy and local-preflight evidence after the cleanup/preflight hardening pass. `python scripts/runtime_artifact_cleanup.py --apply --json` deleted 13 local-only runtime artifact roots, left 7 local-by-default benchmark result roots in `review_local_by_default`, and reported `skipped_tracked=0`; `python scripts/worktree_commit_plan.py --summary-only --json --fail-on-generated --fail-on-cross-cutting --output %TEMP%\qa-z-worktree-plan-l12-compact.json` now reports `generated_artifact_count=0`, `generated_local_only_count=0`, `generated_local_by_default_count=0`, `cross_cutting_count=12`, `unassigned_source_path_count=0`, and `multi_batch_path_count=0`; the include-ignored companion keeps ignored evidence reviewable with `generated_artifact_count=17`, `generated_local_only_count=10`, and `generated_local_by_default_count=7`; and local-only remote preflight with the configured expected origin passed with `release_path_state=local_only_remote_preflight` plus `remote_readiness=ready_for_remote_checks`. `mypy.ini` is now a tracked config surface that pins mypy cache output to `$TEMP/qa-z-mypy-cache`, so the next local-release package should focus on patch-add ownership for the 12 cross-cutting surfaces and leave the remote publish blocker parked until `qazedhq/qa-z` is created or exposed.
- 2026-04-24: L37 rechecked the remote publish blocker again before attempting any further release-stage work. Fresh evidence was unchanged: `python scripts/alpha_release_preflight.py --repository-url https://github.com/qazedhq/qa-z.git --expected-origin-url https://github.com/qazedhq/qa-z.git --allow-dirty --json --output .qa-z/tmp/alpha-release-preflight-remote-live.json` still failed with `release_path_state=blocked_repository`, `remote_readiness=needs_repository_bootstrap`, and `remote_blocker=repository_missing`; `git ls-remote --refs https://github.com/qazedhq/qa-z.git` still returned `remote: Repository not found.`; GitHub repository search still returned no visible `qazedhq/qa-z`; installed accounts still show only `ggbu75769-dot`; and visible org membership is still empty. Because there is still no repo-local fix to apply honestly, this slice stayed in blocker-management mode and wrote the repeated probe evidence back into current-state, blocker-plan, and release handoff docs rather than pretending Stage 4-5 could move. The next release slice remains external: create or expose `qazedhq/qa-z`, or install/authorize access to that owner for this session, then rerun remote preflight before attempting push, PR, or tag cutover.
- 2026-04-24: L36 closed the live remote publish-path blocker recheck. `git remote -v` still matches `https://github.com/qazedhq/qa-z.git`, and fresh direct remote evidence from `python scripts/alpha_release_preflight.py --repository-url https://github.com/qazedhq/qa-z.git --expected-origin-url https://github.com/qazedhq/qa-z.git --allow-dirty --json --output .qa-z/tmp/alpha-release-preflight-remote-live.json` still failed with `release_path_state=blocked_repository`, `remote_readiness=needs_repository_bootstrap`, and `remote_blocker=repository_missing`. The package then checked GitHub account visibility directly: repository search for `qazedhq qa-z` returned no visible repository, installed accounts still show only `ggbu75769-dot`, and visible org membership is empty, so the blocker remains `EXTERNAL + ACCESS` rather than a new repo-local release gap. A secondary repo-local blocker appeared while refreshing current-truth counts: the first `alpha_release_gate` rerun failed only on `ruff_format` because the touched current-truth test files were not yet reformatted. That blocker resolved after `python -m ruff format tests/test_current_truth.py tests/test_current_truth_release_handoff.py tests/test_current_truth_release_surfaces.py`, another full `python -m pytest` (`1158 passed`), and `python scripts/alpha_release_gate.py --allow-dirty --json --output .qa-z/tmp/alpha-release-gate-l36.json`, which passed locally again with `1158 passed`, benchmark `54/54 fixtures, overall_rate 1.0`, mypy green, Ruff green, build green, artifact smoke green, and bundle manifest green. This slice intentionally stopped at the honest boundary and wrote the blocker back into current-state, release handoff, and the new 2026-04-24 plan doc instead of pretending publish could advance. The next release slice is unchanged in substance but now more explicit operationally: create or expose `qazedhq/qa-z`, or install/authorize access to that owner for this session, rerun remote preflight, and only then choose push, PR, or tag cutover.
- 2026-04-23: L35 closed the configured-origin release-gate follow-through by adding a live-like GitHub 404 regression to the preflight remote split pack, fixing `scripts/alpha_release_preflight_evidence.py` so `status_code=404` no longer collapses to `404404`, and reordering the repository blocker classifier so real GitHub `404 Not Found` responses resolve to `remote_blocker=repository_missing` instead of the wrong-target path triggered by `documentation_url`. The first blocker was `TEST`: the new regression pushed `tests/test_alpha_release_preflight_remote.py` over its split budget, which in turn made `python scripts/alpha_release_gate.py --allow-dirty --json --output .qa-z/tmp/alpha-release-gate-l35.json` fail on `ruff_format` plus the remote split architecture check. That blocker resolved after moving the new regression into `tests/test_alpha_release_preflight_remote_refs.py`, pinning the location in `tests/test_alpha_release_preflight_architecture.py`, and formatting the touched files. Fresh release evidence now shows `python scripts/alpha_release_gate.py --allow-dirty --json --output .qa-z/tmp/alpha-release-gate-l35.json` passing locally again with nested preflight carrying `--expected-origin-url https://github.com/qazedhq/qa-z.git`, while direct remote preflight `python scripts/alpha_release_preflight.py --repository-url https://github.com/qazedhq/qa-z.git --expected-origin-url https://github.com/qazedhq/qa-z.git --allow-dirty --json --output .qa-z/tmp/alpha-release-preflight-remote-l35.json` fails externally with `release_path_state=blocked_repository`, `remote_readiness=needs_repository_bootstrap`, and `remote_blocker=repository_missing`. Verification for the package closed with `python -m pytest -q tests/test_alpha_release_preflight_remote.py tests/test_alpha_release_preflight_remote_refs.py tests/test_alpha_release_preflight_architecture.py -k "github_error_body or remote_is_missing or split_budget or remote_ref_contracts_live_in_split_pack"` (`6 passed`), `python -m pytest -q tests/test_alpha_release_preflight_remote.py tests/test_alpha_release_preflight_remote_refs.py tests/test_alpha_release_preflight_architecture.py tests/test_alpha_release_gate.py tests/test_alpha_release_gate_options.py tests/test_alpha_release_gate_cli.py tests/test_alpha_release_gate_evidence.py tests/test_alpha_release_preflight.py` (`86 passed`), `python scripts/alpha_release_gate.py --allow-dirty --json --output .qa-z/tmp/alpha-release-gate-l35.json`, and `python scripts/alpha_release_preflight.py --repository-url https://github.com/qazedhq/qa-z.git --expected-origin-url https://github.com/qazedhq/qa-z.git --allow-dirty --json --output .qa-z/tmp/alpha-release-preflight-remote-l35.json`. The next release slice is unchanged but now better classified: create or expose the intended GitHub repository target, rerun remote preflight, and only then choose push, PR, or tag cutover.
- 2026-04-23: L34 closed release-stageability and pushed through the remote publish path until a real external blocker stopped progress. The package began with TDD around `scripts/worktree_commit_plan_support.py`, adding a failing regression that required `src/qa_z/operator_action_render.py` to join `repair_session_publish`, `tests/test_github_workflow.py` to join `alpha_release_closure`, and `unassigned_source_paths` to reach `[]`; after the helper update plus formatting, `python -m pytest -q tests/test_worktree_commit_plan.py tests/test_worktree_commit_plan_filtering.py tests/test_worktree_commit_plan_output.py tests/test_worktree_commit_plan_architecture.py` passed (`56 passed`). Fresh helper evidence then showed `python scripts/worktree_commit_plan.py --include-ignored --fail-on-generated --json --output .qa-z/tmp/worktree-commit-plan-l34.json` still returning strict generated-artifact attention but with `unassigned_source_path_count=0`, `generated_artifact_count=24`, `generated_local_only_count=17`, and `generated_local_by_default_count=7`; `python scripts/alpha_release_gate.py --allow-dirty --json --output .qa-z/tmp/alpha-release-gate-l34b.json` then passed locally with nested preflight, pytest (`1155 passed`), benchmark (`54/54 fixtures, overall_rate 1.0`), build, artifact smoke, bundle manifest, and CLI help all green. Stage 4 advanced by configuring `origin` to `https://github.com/qazedhq/qa-z.git`, but direct remote preflight `python scripts/alpha_release_preflight.py --repository-url https://github.com/qazedhq/qa-z.git --expected-origin-url https://github.com/qazedhq/qa-z.git --allow-dirty --json --output .qa-z/tmp/alpha-release-preflight-remote-l34.json` failed with `github_repository`, `remote_reachable`, `release_path_state=blocked_repository`, and `remote_readiness=needs_repository_alignment`; GitHub returned `404 Not Found` and `git ls-remote` reported `remote: Repository not found.`. Blocker follow-through confirmed this environment cannot bootstrap that repository directly: `gh` is unavailable, `GITHUB_TOKEN` / `GH_TOKEN` are absent, and the authenticated GitHub connector account is `ggbu75769-dot`, not a visible `qazedhq` organization account. Verification for the package closed with `python -m ruff format --check .`, `python -m qa_z fast --selection smart --json`, `python -m pytest -q tests/test_worktree_commit_plan.py tests/test_worktree_commit_plan_filtering.py tests/test_worktree_commit_plan_output.py tests/test_worktree_commit_plan_architecture.py`, `python scripts/worktree_commit_plan.py --include-ignored --fail-on-generated --json --output .qa-z/tmp/worktree-commit-plan-l34.json`, `python scripts/alpha_release_preflight.py --skip-remote --allow-dirty --json --output .qa-z/tmp/alpha-release-preflight-l34.json`, `python scripts/alpha_release_gate.py --allow-dirty --json --output .qa-z/tmp/alpha-release-gate-l34b.json`, and `python scripts/alpha_release_preflight.py --repository-url https://github.com/qazedhq/qa-z.git --expected-origin-url https://github.com/qazedhq/qa-z.git --allow-dirty --json --output .qa-z/tmp/alpha-release-preflight-remote-l34.json`. The next release slice is now external and explicit: create or expose the intended GitHub repository, or align the target URL, then rerun remote preflight before any push, PR, or tag.
- 2026-04-23: L33 executed Stages 1-2 of the release-readiness outline by carrying loop-local self-inspection into cleanup and workflow prepared actions via `src/qa_z/autonomy_actions.py` plus `src/qa_z/autonomy_action_cleanup.py`, then rerunning the deterministic release helper path against fresh continuity and current-truth evidence. The first blocker was `TEST`: `tests/test_current_truth_architecture.py` failed because the new release continuity assertion overflowed the main current-truth file budget and then also overflowed `tests/test_current_truth_release_surfaces.py`; that blocker resolved after moving the assertion into the new `tests/test_current_truth_release_continuity.py` split pack and updating the architecture mapping. Stage 2 evidence then showed release-stageability is still blocked, but more narrowly than before: `python scripts/worktree_commit_plan.py --include-ignored --fail-on-generated --json --output .qa-z/tmp/worktree-commit-plan-l33.json` reported `generated_artifact_count=24`, `generated_local_only_count=17`, `generated_local_by_default_count=7`, and `unassigned_source_path_count=2`, while `python scripts/alpha_release_preflight.py --skip-remote --allow-dirty --json --output .qa-z/tmp/alpha-release-preflight-l33.json` passed with `remote_readiness=needs_origin_bootstrap`, and `python scripts/alpha_release_gate.py --allow-dirty --json --output .qa-z/tmp/alpha-release-gate-l33.json` failed only on `worktree_commit_plan` attention while nested preflight, `pytest`, benchmark, build, artifact smoke, bundle manifest, and CLI help all passed. Verification closed with `python -m pytest -q tests/test_autonomy_action_cleanup.py tests/test_self_improvement.py tests/test_self_improvement_selection.py tests/test_current_truth.py` (`63 passed`), `python -m pytest -q tests/test_autonomy.py tests/test_autonomy_action_context.py tests/test_autonomy_action_cleanup.py tests/test_self_improvement.py tests/test_self_improvement_inspection.py tests/test_self_improvement_selection.py tests/test_self_improvement_selection_output.py tests/test_current_truth.py` (`122 passed`), `python -m pytest` (`1154 passed`), `python -m qa_z benchmark --json --results-dir %TEMP%\\qa-z-l33-full-benchmark` (`54/54 fixtures, overall_rate 1.0`), `python -m mypy src tests` (`Success: no issues found in 498 source files`), `python -m ruff check .`, `python -m ruff format --check .` (`1014 files already formatted`), `python scripts/worktree_commit_plan.py --include-ignored --fail-on-generated --json --output .qa-z/tmp/worktree-commit-plan-l33.json`, `python scripts/alpha_release_preflight.py --skip-remote --allow-dirty --json --output .qa-z/tmp/alpha-release-preflight-l33.json`, and `python scripts/alpha_release_gate.py --allow-dirty --json --output .qa-z/tmp/alpha-release-gate-l33.json`. The next release slice is concrete: route `src/qa_z/operator_action_render.py` plus `tests/test_github_workflow.py` into an owned commit batch, rerun `alpha_release_gate`, and only then bootstrap `origin` for remote preflight.
- 2026-04-23: L32 closed the next non-release Priority 2 evidence-policy maintenance slice by reclassifying dirty `benchmarks/results/summary.json`, `benchmarks/results/report.md`, and `benchmarks/results-*` paths as `benchmark` area evidence inside live `dirty_area_summary`, then teaching the policy-explicit check to require the stronger `local-only runtime artifacts` plus `local-by-default benchmark evidence` wording. The in-loop blockers were a stale area-classification seam and one downstream pressure helper gap: dirty benchmark result roots were already kept out of `runtime_artifact_paths`, but `classify_worktree_path_area()` still folded them back into `runtime_artifact`, and `has_commit_isolation_worktree_pressure()` still counted those review-only benchmark roots as source-facing drift. Both blockers resolved after excluding local-by-default benchmark evidence from commit-isolation/integration pressure checks, updating the old benchmark-area regression to the new contract, and syncing README/schema/current-state/roadmap continuity wording. Verification closed with `python -m pytest -q tests/test_self_improvement.py tests/test_self_improvement_reseed_policy.py tests/test_self_improvement_live_repository.py tests/test_current_truth.py` (`73 passed`), `python -m pytest` (`1152 passed`), `python -m qa_z benchmark --json --results-dir %TEMP%\\qa-z-l32-full-benchmark` (`54/54 fixtures, overall_rate 1.0`), `python -m mypy src tests` (`Success: no issues found in 497 source files`), `python -m ruff check .`, and `python -m ruff format --check .` (`862 files already formatted`). The next remaining Priority 2 slice is narrower: keep autonomy prepared-action `context_paths`, self-inspection provenance fields, and schema/current-truth wording aligned so future policy maintenance does not drift back into continuity-only ambiguity.
- 2026-04-23: L31 closed the release-facing Priority 2 policy split by teaching `scripts/alpha_release_preflight.py` to collapse tracked generated hits to helper-derived roots, then carrying local-only versus local-by-default counts through `scripts/alpha_release_preflight_evidence.py`, `scripts/alpha_release_gate_evidence.py`, README/release handoff/schema wording, and current-truth regression. The in-loop blocker was an ambiguity between release preflight and the rest of the repository: worktree planning, runtime cleanup, and live repository signals already distinguished local-only runtime artifacts from local-by-default benchmark evidence, but preflight still flattened tracked generated output into one opaque blocker. A secondary blocker appeared during verification when `tests/test_current_truth_release_surfaces.py` crossed its architecture budget after the new release-surface assertions landed; that blocker resolved after compressing repeated file loads behind a small local helper and rerunning the architecture guard. Verification closed with `python -m pytest -q tests/test_alpha_release_preflight.py tests/test_alpha_release_gate_evidence.py tests/test_artifact_schema.py tests/test_current_truth.py tests/test_current_truth_release_surfaces.py tests/test_current_truth_release_handoff.py` (`93 passed`), `python -m pytest -q tests/test_alpha_release_preflight.py tests/test_alpha_release_preflight_cli_render.py tests/test_alpha_release_preflight_remote.py tests/test_alpha_release_preflight_remote_refs.py tests/test_alpha_release_preflight_architecture.py tests/test_alpha_release_gate.py tests/test_alpha_release_gate_environment.py tests/test_alpha_release_gate_evidence.py tests/test_release_script_environment.py tests/test_artifact_schema.py tests/test_current_truth.py tests/test_current_truth_release_surfaces.py tests/test_current_truth_release_handoff.py` (`162 passed`), `python -m pytest` (`1150 passed`), `python -m mypy src tests` (`Success: no issues found in 497 source files`), `python -m ruff check .`, `python -m ruff format --check .` (`862 files already formatted`), and `python scripts/alpha_release_preflight.py --skip-remote --allow-dirty --json --output .qa-z/tmp/alpha-release-preflight-l31.json` (passed). The direct `python scripts/alpha_release_gate.py --allow-dirty --json --output .qa-z/tmp/alpha-release-gate-l31.json` rerun still failed on `worktree_commit_plan` attention only because the accumulated branch remains dirty and not release-stageable as-is; nested local preflight passed and the compact gate evidence preserved the new tracked-generated split.
- 2026-04-23: L30 closed the next Priority 2 policy-alignment slice by teaching `src/qa_z/live_repository.py` to keep dirty `benchmarks/results-*` snapshot roots in `benchmark_result_paths` and `dirty_benchmark_result_count` instead of collapsing them into `runtime_artifact_paths`, then pinning that contract with new live-repository and self-inspection regressions plus continuity-doc sync. The package blocker was a policy-surface ambiguity: cleanup/worktree helpers already treated `benchmarks/results-*` as review-only local-by-default evidence, but live repository signals and self-inspection still surfaced those dirty snapshot roots as runtime-artifact cleanup pressure. The blocker resolved after separating snapshot roots in live signal collection, confirming the broader self-improvement regression stayed green, and removing stale continuity wording that still described benchmark snapshots as runtime artifacts or repeated the older `30 / 25 / 5` strict-helper snapshot. Verification closed with `python -m pytest -q tests/test_self_improvement_live_repository.py tests/test_self_improvement.py tests/test_self_improvement_lifecycle.py tests/test_self_improvement_report_freshness.py tests/test_self_improvement_reseed_policy.py tests/test_autonomy.py tests/test_cli.py` (`125 passed`), `python -m pytest -q tests/test_current_truth.py` (`40 passed`), `python -m pytest` (`1145 passed`), `python -m mypy src tests` (`Success: no issues found in 497 source files`), `python -m ruff check .`, and `python -m ruff format --check .` (`862 files already formatted`).
- 2026-04-23: L29 closed the documented non-blocked mixed-history Priority 5 gap by committing `executor_dry_run_validation_conflict_repeated_rejected_operator_actions`, pinning the same validation-conflict-plus-repeated-rejected retry contract across dry-run logic, benchmark runtime coverage, session publish/output helpers, README/current-state/roadmap continuity docs, and current-truth regression. The first blocker was a RED pass where the new fixture and docs did not exist yet; the second blocker appeared during broad verification when `tests/test_benchmark_executor_runtime_operator_actions.py` exceeded its architecture budget and `tests/test_verification_publish_session_priority5.py` triggered a mypy optional-iteration error. Both blockers resolved after splitting the new benchmark runtime coverage into `tests/test_benchmark_executor_runtime_priority5.py`, making the publish regression iterate over `summary.executor_dry_run_recommended_actions or []`, formatting the touched files, and rerunning the affected architecture and behavior suites. Verification closed with `python -m pytest -q tests/test_executor_dry_run_logic.py tests/test_benchmark_executor_runtime_operator_actions.py tests/test_verification_publish_session.py tests/test_verification_publish_session_priority5.py tests/test_current_truth.py` (`61 passed`), `python -m qa_z benchmark --fixture executor_dry_run_validation_conflict_repeated_rejected_operator_actions --json --results-dir %TEMP%\\qa-z-l29-validation-rejected` (`1/1 fixtures, overall_rate 1.0`), `python -m pytest -q tests/test_benchmark_executor_runtime_architecture.py tests/test_fast_gate_environment.py tests/test_benchmark_executor_runtime_operator_actions.py tests/test_benchmark_executor_runtime_priority5.py tests/test_verification_publish_session_priority5.py` (`18 passed`), `python -m pytest` (`1143 passed`), `python -m qa_z benchmark --json --results-dir %TEMP%\\qa-z-l29-full-benchmark` (`54/54 fixtures, overall_rate 1.0`), `python -m mypy src tests` (`Success: no issues found in 497 source files`), `python -m ruff check .`, and `python -m ruff format --check .` (`862 files already formatted`).
- 2026-04-23: L28 deepened operator-facing executor diagnostics on outcome/publish Markdown by replacing the flat `Dry-run recommended actions:` summary line with ordered `Action <id>:` lines on repair-session outcome Markdown and GitHub-facing publish summaries, while also pinning a history-fallback blocked mixed-history case in `tests/test_verification_publish_session.py`. The in-loop blockers were architecture-budget regressions after the first implementation pass: `src/qa_z/executor_dry_run_render.py` exceeded its line budget after the shared renderer landed, and `tests/test_verification_publish_session.py` exceeded its file budget after the new blocked mixed-history fallback case was written inline. Both blockers resolved after moving the shared action-line renderer into the new `src/qa_z/operator_action_render.py` module, shrinking the publish test with `executor_attempt()` support helpers, and rerunning the affected architecture and behavior suites. Verification closed with `python -m pytest -q tests/test_repair_session.py tests/test_verification_publish_session.py tests/test_current_truth.py tests/test_executor_dry_run_render.py tests/test_executor_history_dry_run_layout_architecture.py tests/test_verification_publish_architecture.py` (`62 passed`), `python -m pytest` (`1140 passed`), `python -m qa_z benchmark --json --results-dir %TEMP%\\qa-z-l28-final-benchmark` (`53/53 fixtures, overall_rate 1.0`), `python -m mypy src tests` (`Success: no issues found in 495 source files`), `python -m ruff check .`, and `python -m ruff format --check .` (`705 files already formatted`).
- 2026-04-23: L27 closed report/template/example sync by refreshing README repo-map and near-term roadmap wording, pinning current-state and roadmap continuity docs to the shipped workflow-template non-goals for executor-result ingest and autonomous repair, and moving the post-package handoff from "Priority 4 is next" to "Priority 5 executor operator diagnostics is next." The in-loop blocker was continuity drift across `README.md`, `docs/reports/current-state-analysis.md`, and `docs/reports/next-improvement-roadmap.md`: public/docs surfaces still treated report/template/example sync plus standalone GitHub annotation helpers as the next roadmap package even though the sync pass had already landed and extra annotation surfaces remained deferred. The blocker resolved after aligning README/current-state/roadmap wording, extending current-truth coverage to lock the post-sync next-step contract, and rerunning the affected regression. Verification closed with `python -m pytest -q tests/test_current_truth.py tests/test_examples.py tests/test_github_workflow.py tests/test_cli.py` (`90 passed`), `python -m pytest` (`1139 passed`), `python -m qa_z benchmark --json --results-dir %TEMP%\\qa-z-l27-full-benchmark` (`53/53 fixtures, overall_rate 1.0`), `python -m mypy src tests` (`Success: no issues found in 494 source files`), `python -m ruff check .`, and `python -m ruff format --check .` (`704 files already formatted`).
- 2026-04-23: L26 closed multi-source scan warning benchmark breadth by hardening `src/qa_z/benchmark_report_details.py` so deep warning lines fall back from summary-level `scan_quality_*` to raw `scan_warning_*`, adding the executed `mixed_fast_deep_scan_warning_fast_only` fixture to prove non-blocking multi-source deep warnings preserve attached-run lineage while repair handoff stays fast-only, and syncing README/benchmark/current-state/current-truth continuity docs to the broadened corpus. The in-loop blockers were a stale `tests/test_current_truth.py` assertion still pinning README breadth at three executed mixed fast-plus-deep fixtures and a follow-up `ruff format --check .` drift on the touched benchmark/current-truth test files; both blockers resolved after syncing the assertion to the new four-fixture corpus wording, formatting the touched tests, and rerunning the affected regression. Verification closed with `python -m pytest -q tests/test_benchmark_report_details.py tests/test_benchmark_reporting.py tests/test_benchmark_corpus.py` (`23 passed`), `python -m pytest -q tests/test_current_truth.py -k "semgrep_scan_warning_diagnostics_are_documented or mixed_fast_deep_benchmark_breadth_is_documented"` (`2 passed`), `python -m qa_z benchmark --fixture deep_scan_warning_multi_source_diagnostics --json --results-dir %TEMP%\\qa-z-l26-multi-source-warning` (`1/1 fixtures, overall_rate 1.0`), `python -m qa_z benchmark --fixture mixed_fast_deep_scan_warning_fast_only --json --results-dir %TEMP%\\qa-z-l26-mixed-warning-fast-only` (`1/1 fixtures, overall_rate 1.0`), `python -m qa_z benchmark --json --results-dir %TEMP%\\qa-z-l26-full-benchmark` (`53/53 fixtures, overall_rate 1.0`), `python -m pytest -q` (`1133 passed`), `python -m mypy src tests`, `python -m ruff check src/qa_z/benchmark_report_details.py tests/test_benchmark_report_details.py tests/test_benchmark_reporting.py tests/test_benchmark_corpus.py tests/test_current_truth.py`, and `python -m ruff format --check .` (`556 files already formatted`).
- 2026-04-23: L25 closed runtime-artifact cleanup/helper alignment by teaching `scripts/runtime_artifact_cleanup_support.py` to discover candidates from the same generated-policy buckets as strict worktree planning, fixing apply-mode kind reporting so deleted directories stay typed as directories, adding `sys.dont_write_bytecode = True` guards to `scripts/runtime_artifact_cleanup.py` and `scripts/worktree_commit_plan.py` so rerunning those scripts does not recreate `__pycache__` local-only roots, and syncing README/policy/current-state/worktree continuity docs plus current-truth coverage to the post-gate `5 / 0 / 5` snapshot. The in-loop blocker was the first alpha gate rerun failing on `ruff_format` and nested `qa_z_fast`; the blocker resolved after confirming the touched Python files were formatted and rerunning the full gate. Verification closed with `python -B -m pytest -p no:cacheprovider tests/test_runtime_artifact_cleanup.py tests/test_runtime_artifact_cleanup_architecture.py tests/test_worktree_commit_plan_architecture.py tests/test_current_truth.py -q` (`50 passed`), `python -m ruff check --no-cache scripts/runtime_artifact_cleanup.py scripts/runtime_artifact_cleanup_support.py scripts/worktree_commit_plan.py tests/test_runtime_artifact_cleanup.py tests/test_runtime_artifact_cleanup_architecture.py tests/test_worktree_commit_plan_architecture.py tests/test_current_truth.py`, `python scripts/alpha_release_gate.py --allow-dirty --output .qa-z/tmp/alpha-release-gate-l25-rerun.json --json` (passed with `29/29` checks, `1131 passed`, `52/52 fixtures`, `mypy`, build, artifact smoke, and bundle manifest), `python scripts/runtime_artifact_cleanup.py --apply --json` (`deleted=17`, `review_local_by_default=5`), `python scripts/runtime_artifact_cleanup.py --json` (`planned=0`, `review_local_by_default=5`, `deleted=0`), and `python scripts/worktree_commit_plan.py --include-ignored --fail-on-generated --json` (`generated_artifact_count=5`, `generated_local_only_count=0`, `generated_local_by_default_count=5`, `unassigned_source_path_count=0`, `multi_batch_path_count=0`).
- 2026-04-23: L24 closed worktree commit-plan staging-truth hardening by switching the helper runtime to `git status --short --untracked-files=all`, routing modular command wrappers to their owning batches, promoting the shared CLI/command-registration/runtime spine into explicit patch-add surfaces, collapsing generated descendants to root buckets, syncing current-state/worktree continuity docs to the refreshed `31 / 26 / 5` strict snapshot, and fixing the in-loop blocker where `tests/test_alpha_release_gate_evidence.py` still expected pre-bucket file-versus-directory counts while `ruff_format` also caught touched Python files that had not been reformatted yet. The blocker resolved after updating the alpha-release evidence expectation to directory-bucket semantics and rerunning Ruff format on the touched Python surfaces. Verification closed with `python -m pytest -q tests/test_worktree_commit_plan.py tests/test_worktree_commit_plan_filtering.py tests/test_worktree_commit_plan_output.py tests/test_worktree_commit_plan_architecture.py tests/test_current_truth.py` (`89 passed`), `python -m pytest -q tests/test_alpha_release_gate_evidence.py tests/test_worktree_commit_plan.py tests/test_worktree_commit_plan_filtering.py tests/test_worktree_commit_plan_output.py tests/test_worktree_commit_plan_architecture.py tests/test_current_truth.py` (`114 passed`), `python -m pytest -q` (`1129 passed`), `python -m mypy src tests`, `python scripts/worktree_commit_plan.py --json --output .qa-z/tmp/worktree-commit-plan-l24-postfix.json` (`changed_path_count=517`, `cross_cutting_count=12`, `shared_patch_add_count=16`), `python scripts/worktree_commit_plan.py --include-ignored --fail-on-generated --json --output .qa-z/tmp/worktree-commit-plan-strict-l24.json` (`generated_artifact_count=31`, `generated_local_only_count=26`, `generated_local_by_default_count=5`, `unassigned_source_path_count=0`), and the final `python scripts/alpha_release_gate.py --allow-dirty --output .qa-z/tmp/alpha-release-gate-l24-rerun.json --json` rerun (passed with `29/29` checks, `1129 passed`, `52/52 fixtures`).
- 2026-04-23: L23 closed runtime-artifact cleanup safety alignment by splitting `scripts/runtime_artifact_cleanup_support.py` into local-only versus local-by-default policy buckets, making `benchmarks/results/` plus `benchmarks/results-*` stay `review_local_by_default` even under `--apply`, syncing README/current-truth/generated-policy/worktree docs to that contract, and documenting the in-loop blocker where the first alpha gate rerun surfaced `ruff_format` plus nested `qa_z_fast` failure from the same unformatted `tests/test_current_truth.py` drift; the blocker resolved after formatting that file and rerunning the gate. Verification closed with `python -m pytest -q` (`1126 passed`), `python -m ruff check scripts/runtime_artifact_cleanup.py scripts/runtime_artifact_cleanup_support.py tests/test_runtime_artifact_cleanup.py tests/test_current_truth.py`, `python -m mypy src tests`, `python scripts/runtime_artifact_cleanup.py --json` (`planned=1`, `review_local_by_default=5`, `skipped_tracked=0`, `deleted=0`), `python scripts/worktree_commit_plan.py --include-ignored --fail-on-generated --json --output .qa-z/tmp/worktree-commit-plan-strict-l23.json` (`generated_artifact_count=30`, `generated_local_only_count=25`, `generated_local_by_default_count=5`, `unassigned_source_path_count=0`), and the final `python scripts/alpha_release_gate.py --allow-dirty --output .qa-z/tmp/alpha-release-gate-l23-rerun.json --json` rerun (passed with `29/29` checks).
- 2026-04-23: L22 closed the generated-artifact policy bucket split by teaching `scripts/worktree_commit_plan_support.py` to distinguish local-only runtime artifact roots from local-by-default benchmark evidence roots, propagating those counts through `scripts/alpha_release_gate_evidence.py`, syncing README/schema/current-truth/policy docs, and fixing two closure blockers in the same package: the initial test-split budget overrun was resolved by moving the new output-focused regression into `tests/test_worktree_commit_plan_output.py`, and the follow-up current-truth drift was resolved by refreshing `tests/test_current_truth.py` to the new `1126 passed` baseline plus the new generated-policy bucket fields; verification closed with `python -m pytest -q tests/test_current_truth.py tests/test_artifact_schema.py tests/test_worktree_commit_plan.py tests/test_worktree_commit_plan_output.py tests/test_alpha_release_gate_evidence.py tests/test_alpha_release_gate_render.py` (`133 passed`), `python -m pytest -q tests/test_worktree_commit_plan_architecture.py tests/test_worktree_commit_plan.py tests/test_worktree_commit_plan_output.py` (`46 passed`), `python scripts/worktree_commit_plan.py --include-ignored --fail-on-generated --json --output .qa-z/tmp/worktree-commit-plan-strict-l22.json` (`generated_artifact_count=30`, `generated_local_only_count=25`, `generated_local_by_default_count=5`, `unassigned_source_path_count=0`), and the final `python scripts/alpha_release_gate.py --allow-dirty --output .qa-z/tmp/alpha-release-gate-l22.json --json` rerun (passed with `29/29` checks, `1126 passed`, `52/52 fixtures`).
- 2026-04-23: L21 closed the remaining worktree subprocess/tooling ownership gap by routing the current Ruff/tool-cache `pyproject.toml` delta plus `src/qa_z/subprocess_env.py` into the planning/runtime foundation batch, moving `src/qa_z/runners/subprocess.py` under the shared runner contract spine, assigning `tests/test_release_script_environment.py` to the alpha release closure lane, and widening the affected batch validation commands so the helper no longer leaves those surfaces as `unassigned_source_paths`; verification closed with `python -m pytest -q tests/test_worktree_commit_plan.py tests/test_worktree_commit_plan_filtering.py tests/test_worktree_commit_plan_output.py tests/test_worktree_commit_plan_architecture.py tests/test_release_script_environment.py tests/test_fast_gate_environment.py` plus `python scripts/worktree_commit_plan.py --json --output .qa-z/tmp/worktree-commit-plan.json` and `python scripts/worktree_commit_plan.py --include-ignored --fail-on-generated --json --output .qa-z/tmp/worktree-commit-plan-strict.json`.
- 2026-04-23: L20 checkpoint refresh revalidated the documented import-topology baseline against the current dirty worktree and synchronized the continuity docs to the fresh gate evidence; verification closed with `python -m pytest -q` (`1122 passed`), `python -m qa_z benchmark --json --results-dir benchmarks/results-l20-refresh` (`52/52 fixtures, overall_rate 1.0`), `python -m ruff check .`, `python -m ruff format --check .` (`554 files already formatted`), `python -m mypy src tests` (`494 source files`), and `python -m qa_z --help`.
- 2026-04-23: L19 closed release current-truth handoff split by moving commit-split and publish-handoff assertions into `tests/test_current_truth_release_handoff.py`, extending `tests/test_current_truth_architecture.py` to require that new handoff pack, and shrinking `tests/test_current_truth_release_surfaces.py` to `657` lines so frozen release metadata stays separate from remote-blocker handoff contracts; verification closed with `python -m pytest -q tests/test_current_truth.py tests/test_current_truth_release_surfaces.py tests/test_current_truth_release_handoff.py tests/test_current_truth_architecture.py` (`42 passed`) and `python -m ruff check tests/test_current_truth_release_surfaces.py tests/test_current_truth_release_handoff.py tests/test_current_truth_architecture.py`.
- 2026-04-23: L18 closed alpha-release preflight remote-pack decomposition by moving origin-mismatch, existing-ref, release-tag, and repository-target blocker coverage into `tests/test_alpha_release_preflight_remote_refs.py`, extending `tests/alpha_release_preflight_test_support.py` plus the architecture guard to require that new split pack, and shrinking `tests/test_alpha_release_preflight_remote.py` to `526` lines so the core remote/probe path stays separate from ref/blocker edge cases; verification closed with `python -m pytest -q tests/test_alpha_release_preflight_remote.py tests/test_alpha_release_preflight_remote_refs.py tests/test_alpha_release_preflight_cli_render.py tests/test_alpha_release_preflight_architecture.py tests/test_alpha_release_preflight.py` (`56 passed`) and `python -m ruff check tests/test_alpha_release_preflight_remote.py tests/test_alpha_release_preflight_remote_refs.py tests/test_alpha_release_preflight_architecture.py tests/alpha_release_preflight_test_support.py`.
- 2026-04-23: L17 closed alpha-release gate test-pack decomposition by moving remote/preflight-output and worktree-output coverage into `tests/test_alpha_release_gate_options.py`, moving CLI wrapper coverage into `tests/test_alpha_release_gate_cli.py`, extending `tests/alpha_release_gate_test_support.py` and the architecture guard to require those split packs, and shrinking the main `tests/test_alpha_release_gate.py` to `463` lines so runtime regression is isolated from option and CLI surfaces; verification closed with `python -m pytest -q tests/test_alpha_release_gate.py tests/test_alpha_release_gate_options.py tests/test_alpha_release_gate_cli.py tests/test_alpha_release_gate_evidence.py tests/test_alpha_release_gate_render.py tests/test_alpha_release_gate_architecture.py` (`70 passed`) and `python -m ruff check tests/test_alpha_release_gate.py tests/test_alpha_release_gate_options.py tests/test_alpha_release_gate_cli.py tests/test_alpha_release_gate_architecture.py tests/alpha_release_gate_test_support.py`.
- 2026-04-23: L16 closed release-script subprocess hardening by routing `scripts/alpha_release_preflight.py`, `scripts/alpha_release_artifact_smoke_support.py`, `scripts/alpha_release_bundle_manifest_support.py`, `scripts/runtime_artifact_cleanup.py`, and `scripts/worktree_commit_plan.py` through the shared QA-Z subprocess environment helper so UTF-8 decoding and safe tool-cache handling are consistent across the release/preflight/cleanup script lane, then locking that behavior with `tests/test_release_script_environment.py`; verification closed with `python -m pytest -q tests/test_release_script_environment.py tests/test_alpha_release_preflight.py tests/test_alpha_release_artifact_smoke.py tests/test_alpha_release_bundle_manifest.py tests/test_runtime_artifact_cleanup.py tests/test_worktree_commit_plan.py tests/test_worktree_commit_plan_output.py tests/test_worktree_commit_plan_filtering.py tests/test_worktree_commit_plan_architecture.py` (`70 passed`), `python -m ruff check scripts/alpha_release_preflight.py scripts/alpha_release_artifact_smoke_support.py scripts/alpha_release_bundle_manifest_support.py scripts/runtime_artifact_cleanup.py scripts/worktree_commit_plan.py tests/test_release_script_environment.py`, `python scripts/alpha_release_preflight.py --allow-dirty --skip-remote --json` (passed), and `python scripts/runtime_artifact_cleanup.py --json`.
- 2026-04-23: L15 closed Ruff cache-path hardening by adding shared subprocess environment handling in `src/qa_z/subprocess_env.py`, routing both fast-runner subprocesses and `scripts/alpha_release_gate.py` through that helper, pinning Ruff's repo-local cache directory in `pyproject.toml`, teaching worktree commit-plan generated-artifact detection plus `.gitignore`/policy/current-truth surfaces about `.ruff_cache_safe/`, and hardening the direct Ruff regression test to read UTF-8 subprocess output without cp949 decode noise; checkpoint verification closed with `python -m pytest -q tests/test_fast_gate_environment.py tests/test_alpha_release_gate.py tests/test_alpha_release_gate_environment.py tests/test_current_truth.py tests/test_worktree_commit_plan.py tests/test_worktree_commit_plan_output.py tests/test_worktree_commit_plan_filtering.py tests/test_worktree_commit_plan_architecture.py` (`111 passed`), `python -m ruff check .`, `python -m ruff format --check .` (`549 files already formatted`), and `python -m qa_z fast --selection smart --json` (`1111 passed, 1 skipped`, `489 source files`) while keeping direct Ruff stderr free of the prior cache access-denied warning.
- 2026-04-23: L14 closed the first worktree commit-plan giant-test split by moving output-focused coverage into `tests/test_worktree_commit_plan_output.py`, moving batch-filtering and CLI/repo-context coverage into `tests/test_worktree_commit_plan_filtering.py`, shrinking the main `tests/test_worktree_commit_plan.py` down to a guarded post-split budget, and hardening the seam with `tests/test_worktree_commit_plan_architecture.py` so the shared support module and split budget stay enforced; live verification closed with `python -m pytest -q tests/test_worktree_commit_plan.py tests/test_worktree_commit_plan_output.py tests/test_worktree_commit_plan_filtering.py tests/test_worktree_commit_plan_architecture.py` (`51 passed`), `python -m pytest -q` (`1106 passed, 1 skipped`), `python -m ruff check .`, `python -m ruff format --check .` (`547 files already formatted`), and `python scripts/worktree_commit_plan.py --json --output .qa-z/tmp/worktree-commit-plan.json` still showing `unassigned_source_path_count=0`, `multi_batch_path_count=0`, `batch_count=11`, and `shared_patch_add_count=8`.
- 2026-04-23: L13 closed autonomy action seam extraction by splitting `autonomy_actions.py` into `autonomy_action_context.py`, `autonomy_action_cleanup.py`, `autonomy_action_sessions.py`, and `autonomy_action_packets.py`, moving loop-context, cleanup packet, and repair-session evidence helpers out of the orchestrator while adding direct context/cleanup/session regression packs plus an architecture guard that keeps those helpers from collapsing back into the monolith; live verification closed with `python -m pytest -q tests/test_autonomy_action_context.py tests/test_autonomy_action_cleanup.py tests/test_autonomy_action_sessions.py tests/test_autonomy_actions_architecture.py tests/test_autonomy.py` (`46 passed`), `python -m mypy src tests` (`485 source files`), `python -m pytest -q` (`1105 passed, 1 skipped`), `python -m ruff check .`, `python -m ruff format --check .` (`545 files already formatted`), `python -m qa_z autonomy --help`, and `python -m qa_z --help`.
- 2026-04-23: L12 closed worktree patch-add guidance plus Windows mypy gate hardening by promoting combined cross-cutting and report-file candidates into `shared_patch_add_paths`, surfacing `shared_patch_add_count` through worktree-plan human/JSON output and alpha-release gate evidence/render, then fixing the `python -m mypy src tests` / `mypy src tests` access-violation regression with a root `mypy.ini` that pins the cache to `$TEMP/qa-z-mypy-cache`; live verification showed `python scripts/worktree_commit_plan.py --json --output .qa-z/tmp/worktree-commit-plan.json` still reaching `unassigned_source_path_count=0`, `multi_batch_path_count=0`, `batch_count=11`, and `shared_patch_add_count=8`, while regression closed with `python -m pytest -q tests/test_worktree_commit_plan.py tests/test_alpha_release_gate.py tests/test_alpha_release_gate_evidence.py tests/test_alpha_release_gate_render.py tests/test_fast_gate_environment.py tests/test_current_truth.py tests/test_artifact_schema.py` (`159 passed`), `python -m mypy src tests`, `mypy src tests`, `python -m pytest -q` (`1096 passed, 1 skipped`), `python -m ruff check .`, `python -m ruff format --check .` (`537 files already formatted`), and `python -m qa_z --help`.
- 2026-04-23: L11 closed final ownership closure for the remaining worktree helper gaps by adding a dedicated `runner_contract_spine` batch for `src/qa_z/runners/models.py` plus new direct contract/architecture tests, routing `tests/test_fast_gate_environment.py` into the planning/runtime foundation batch, and documenting the new shared-runner ownership rule in the worktree plan report; the package also hardened `qa_z.runners.models` with an explicit `__all__` surface so the shared runner contract is no longer implicit, and live verification showed `python scripts/worktree_commit_plan.py --json --output .qa-z/tmp/worktree-commit-plan.json` reaching `unassigned_source_path_count=0`, `multi_batch_path_count=0`, and `batch_count=11`, while regression closed with `python -m pytest -q tests/test_runner_models_contract.py tests/test_runner_models_architecture.py tests/test_worktree_commit_plan.py tests/test_worktree_commit_plan_architecture.py tests/test_alpha_release_gate_evidence.py tests/test_alpha_release_gate_render.py` (`100 passed`), `python -m pytest -q` (`1095 passed`), `python -m mypy src tests`, `python -m ruff check .`, `python -m ruff format --check .` (`536 files already formatted`), and `python -m qa_z --help`.
- 2026-04-23: L10 closed the checkpoint deep-runner ownership package by adding a dedicated `deep_runner_foundation` batch for the split deep/semgrep runner seams and routing generic support files plus residual executor/self-inspection tests into owned batches, while explicitly leaving `src/qa_z/runners/models.py` outside that batch per reviewer guidance because it is still a shared runner model spine; live verification showed `python scripts/worktree_commit_plan.py --json --output .qa-z/tmp/worktree-commit-plan.json` reducing the remaining unassigned set from `20` to just `src/qa_z/runners/models.py` and `tests/test_fast_gate_environment.py` with `multi_batch_path_count=0`, and the checkpoint closed with reviewer feedback integrated, `python -m pytest -q tests/test_worktree_commit_plan.py tests/test_worktree_commit_plan_architecture.py tests/test_alpha_release_gate_evidence.py tests/test_alpha_release_gate_render.py` (`93 passed`), `python -m pytest -q` (`1088 passed`), `python -m mypy src tests`, `python -m ruff check .`, `python -m ruff format --check .` (`534 files already formatted`), and `python -m qa_z --help`.
- 2026-04-23: L9 closed alpha-release support ownership by extending the alpha release closure batch to absorb artifact-smoke and bundle-manifest scripts, tests, and support helpers, then syncing the worktree plan report so those release-proof surfaces stage with the gate/preflight slice they validate; live verification showed `python scripts/worktree_commit_plan.py --json --output .qa-z/tmp/worktree-commit-plan.json` cutting `unassigned_source_path_count` from `29` to `20` while keeping `multi_batch_path_count=0`, and regression closed with `python -m pytest -q tests/test_worktree_commit_plan.py tests/test_worktree_commit_plan_architecture.py` (`45 passed`), `python -m pytest -q` (`1086 passed`), `python -m mypy src tests`, `python -m ruff check .`, `python -m ruff format --check .` (`534 files already formatted`), and `python -m qa_z --help`.
- 2026-04-23: L8 closed planning/runtime foundation ownership by adding a dedicated `planning_runtime_foundation` batch for CLI, command-registry, live-repository, execution-candidate, and report/runtime seams, extending owner overrides so self-inspection discovery files stay on the backlog side, and updating alpha-release gate expectations plus the worktree plan report to the new 9-batch topology; live verification showed `python scripts/worktree_commit_plan.py --json --output .qa-z/tmp/worktree-commit-plan.json` holding `multi_batch_path_count=0` while cutting `unassigned_source_path_count` from `64` to `29`, and regression closed with `python -m pytest -q tests/test_worktree_commit_plan.py tests/test_worktree_commit_plan_architecture.py tests/test_alpha_release_gate_evidence.py tests/test_alpha_release_gate_render.py` (`90 passed`), `python -m pytest -q` (`1085 passed`), `python -m mypy src tests`, `python -m ruff check .`, `python -m ruff format --check .` (`534 files already formatted`), and `python -m qa_z --help`.
- 2026-04-23: L7 closed worktree commit-plan classification hardening by teaching the helper to deterministically own verification/reporting seams under the verification-and-publish batch, routing known executor fixture trees and signal overlap files through explicit owner overrides, and updating the worktree report so the helper now reflects durable batch ownership instead of noisy false overlap; live verification showed `python scripts/worktree_commit_plan.py --json --output .qa-z/tmp/worktree-commit-plan.json` dropping `multi_batch_path_count` from `11` to `0` and `unassigned_source_path_count` from `164` to `64`, while regression closed with `python -m pytest -q tests/test_worktree_commit_plan.py tests/test_worktree_commit_plan_architecture.py` (`43 passed`), `python -m pytest -q` (`1084 passed`), `python -m mypy src tests`, `python -m ruff check .`, `python -m ruff format --check .` (`534 files already formatted`), and `python -m qa_z --help`.
- 2026-04-23: L6 closed the deterministic worktree commit-plan runtime package by restoring the missing CLI entrypoint on `scripts/worktree_commit_plan.py`, hardening subprocess-based runtime tests plus an architecture guard around that entrypoint, and pushing the repo-standard worktree-plan command to the artifact-writing form `python scripts/worktree_commit_plan.py --json --output .qa-z/tmp/worktree-commit-plan.json` across autonomy, selection output, README, schema, and the worktree plan report so operator handoff now leaves durable local evidence instead of a silent no-op; verification closed with the live helper run `python scripts/worktree_commit_plan.py --json --output .qa-z/tmp/worktree-commit-plan.json`, `python -m pytest -q tests/test_worktree_commit_plan.py tests/test_worktree_commit_plan_architecture.py tests/test_autonomy.py tests/test_self_improvement_selection_output.py tests/test_cli.py tests/test_current_truth.py` (`172 passed`), `python -m pytest -q` (`1082 passed`), `python -m mypy src tests`, `python -m ruff check .`, `python -m ruff format --check .` (`534 files already formatted`), and `python -m qa_z --help`.
- 2026-04-23: L5 closed repeated-fallback concrete-first recovery by penalizing the initial repeated cleanup-family loop-health gap when a concrete non-loop-health alternative family is present, locking that ordering into selection-history regression, and confirming the end-to-end effect with a real autonomy loop so `qa-z self-inspect --json` dropped `autonomy_selection_gap-repeated-fallback-cleanup` and left only the concrete worktree integration tasks open; verification closed with reviewer feedback integrated, `python -m pytest -q tests/test_self_improvement_selection_history.py -k "surfaces_non_repeated_family_after_fallback_diversity_task or keeps_cleanup_family_when_no_alternative_family_exists or penalizes_repeated_fallback_family_when_alternative_exists or derives_autonomy_selection_gap_from_repeated_fallback_family"` (`4 passed`), `python -m qa_z autonomy --loops 1 --json`, `python -m qa_z self-inspect --json`, `python -m qa_z select-next --json`, `python -m pytest -q` (`1079 passed`), `python -m mypy src tests`, `python -m ruff check .`, `python -m ruff format --check .` (`534 files already formatted`), and `python -m pytest -q tests/test_current_truth.py` (`32 passed`).
- 2026-04-23: L4 closed the report-freshness timezone drift package by teaching report freshness to accept date lines that fall within the possible local calendar window of a timezone-aware `generated_at`, then locking that behavior with direct freshness tests plus worktree-discovery coverage so same-day KST reports no longer disappear from UTC-stamped self-inspection loops; verification closed with `python -m pytest -q tests/test_report_freshness.py tests/test_worktree_discovery_candidates.py` (`5 passed`), live `python -m qa_z self-inspect --json` (both `integration_gap` and `worktree_risk` now carry fresh `current_state` report evidence), `python -m pytest -q` (`1079 passed`), `python -m mypy src tests`, `python -m ruff check .`, and `python -m ruff format --check .` (`534 files already formatted`).
- 2026-04-23: L3 connected the live `worktree_risk` path to the repo-standard commit-plan workflow by letting fresh self-inspection candidate evidence absorb matching worktree report context, adding `python scripts/worktree_commit_plan.py --json` plus `docs/reports/worktree-commit-plan.md` to `reduce_integration_risk` prepared actions and human hints, and syncing README/schema current-truth wording so dirty-worktree follow-through now points at the actual split helper instead of a generic cleanup-only path; verification closed with `python -m pytest -q tests/test_worktree_discovery_candidates.py tests/test_autonomy.py tests/test_self_improvement_selection_output.py tests/test_current_truth.py` (`91 passed`), `python -m qa_z self-inspect --json`, `python -m qa_z select-next --json`, `python -m qa_z autonomy --loops 1 --json`, `python -m qa_z autonomy status --json`, `python -m pytest -q` (`1076 passed`), `python -m mypy src tests`, `python -m ruff check .`, `python -m ruff format --check .` (`533 files already formatted`), `python -m qa_z benchmark --json --results-dir benchmarks/results-l3-worktree-risk` (`52/52 fixtures, overall_rate 1.0`), and `python -m qa_z --help`.
- 2026-04-23: L2 closed concrete-backlog reseed persistence by keeping `backlog_reseeding_gap` as a self-inspection diagnostic while persisting only concrete reseeded items into `.qa-z/improvement/backlog.json`, recording `backlog_reseeded` plus `reseeded_candidate_ids` on `self_inspect.json`, surfacing that reseed status on human self-inspect output, and syncing README/schema current-truth wording so `select-next` stays focused on actionable work after an empty backlog recovery; verification closed with `python -m pytest -q tests/test_self_improvement_reseed_policy.py tests/test_self_improvement_selection.py tests/test_cli.py tests/test_current_truth.py` (`83 passed`), `python -m ruff check src/qa_z/backlog_core.py src/qa_z/self_improvement_inspection.py src/qa_z/commands/planning_output.py tests/test_self_improvement_reseed_policy.py tests/test_cli.py tests/test_current_truth.py`, `python -m qa_z self-inspect --json` (`backlog_reseeded=false`, only `worktree_risk` candidate live), `python -m qa_z backlog --json` (synthetic reseed item closed), and `python -m qa_z select-next --json` (single concrete `worktree_risk` selection with preserved live repository context).
- 2026-04-23: L1 closed generated-runtime snapshot cleanup alignment by extending the explicit artifact policy, self-inspection policy snapshot, benchmark reporting guidance, and root `.gitignore` contract to cover local snapshot directories matching `benchmarks/results-*`, then applying `python scripts/runtime_artifact_cleanup.py --apply --json` so stale isolated benchmark outputs no longer reopen artifact-hygiene drift; verification closed with `python -m pytest -q tests/test_current_truth.py -k "gitignore or generated_vs_frozen_policy"` (`2 passed`), `python -m pytest -q tests/test_self_improvement_live_repository.py -k "generated_artifact_policy_snapshot_requires_policy_doc or results_snapshot_ignore_rule"` (`2 passed`), `python -m pytest -q tests/test_benchmark_reporting.py -k generated_policy` (`1 passed`), `python -m ruff check src/qa_z/live_repository.py src/qa_z/benchmark_report_details.py tests/self_improvement_test_support.py tests/test_current_truth.py tests/test_self_improvement_live_repository.py tests/test_benchmark_reporting.py`, `python -m qa_z self-inspect --json` (runtime artifacts and dirty benchmark result count fell to `0`), `python -m qa_z backlog`, and `python -m qa_z select-next`.
- 2026-04-23: L20 closed verification import-topology hardening by replacing `verification_compare.py`, `verification_findings.py`, and `verification_models.py` importlib seams with explicit imports plus public surface contracts, updating benchmark/verification architecture expectations to the new direct-import pattern, tightening verification typing through the comparison builder and executor-ingest verdict handoff, and adding a same-process verification import smoke so split modules stay cycle-safe; verification closed with an independent reviewer pass plus `python -m ruff check .`, `python -m mypy src tests`, `python -m pytest -q` (`1070 passed`), `python -m qa_z benchmark --json --results-dir benchmarks/results-l20-final` (`52/52 fixtures, overall_rate 1.0`), `python -m ruff format --check .` (`1556 files already formatted`), and `python -m qa_z --help`.
- 2026-04-23: L19 closed benchmark import-topology hardening by replacing `benchmark_execution.py`, `benchmark_executor_execution.py`, `benchmark_runtime.py`, and `benchmark_summaries.py` importlib seams with explicit imports plus `__all__`/direct wrapper contracts, then locking those split modules with no-importlib architecture guards and widening regression across benchmark runtime, reporting, corpus, and current-truth consumers; verification closed with `python -m pytest -q tests/test_benchmark_execution_architecture.py tests/test_benchmark_runtime_architecture.py tests/test_benchmark_summaries_architecture.py tests/test_benchmark_runtime.py tests/test_benchmark_reporting.py tests/test_benchmark.py tests/test_current_truth.py tests/test_current_truth_release_surfaces.py` (`74 passed`), `python -m qa_z benchmark --json --results-dir benchmarks/results-l19-benchmark` (`52/52 fixtures, overall_rate 1.0`), and `python -m ruff check src/qa_z/benchmark_execution.py src/qa_z/benchmark_executor_execution.py src/qa_z/benchmark_runtime.py src/qa_z/benchmark_summaries.py tests/test_benchmark_execution_architecture.py tests/test_benchmark_runtime_architecture.py tests/test_benchmark_summaries_architecture.py`.
- 2026-04-23: L18 closed benchmark/current-truth refresh by syncing `executor_dry_run_completed_verify_blocked` with the blocked verification-plus-validation-conflict dry-run contract, adding direct benchmark regression coverage for that fixture, refreshing the live current-state/roadmap gate snapshot to the fresh local evidence, and reformatting lingering Python surfaces so the formatting gate is honest again; verification closed with `python -m pytest -q tests/test_benchmark_executor_runtime_operator_actions.py tests/test_benchmark_executor_runtime_blocked.py tests/test_benchmark_executor_runtime_architecture.py tests/test_benchmark_corpus.py tests/test_executor_dry_run_logic.py tests/test_executor_result_dry_run.py tests/test_repair_prompt.py tests/test_repair_prompt_contracts.py tests/test_repair_prompt_architecture.py tests/test_current_truth.py` (`58 passed`), `python -m qa_z benchmark --json --results-dir benchmarks/results-l18-full` (`52/52 fixtures, overall_rate 1.0`), `python -m ruff format .`, `python -m ruff format --check .` (`1268 files already formatted`), `python -m ruff check .`, `python -m mypy src tests`, `python -m pytest -q` (`1064 passed`), and `python -m qa_z --help`.
- 2026-04-23: L17 closed deep-runner facade hardening by replacing `qa_z.runners.deep` importlib re-exports with explicit imports plus `__all__`, moving `deep_runtime.py` to type-only `DeepRun` imports to avoid wrapper cycles, and widening regression across deep selection, deep run-resolution, and Semgrep normalization consumers; verification closed with `python -m pytest -q tests/test_deep_runner_architecture.py tests/test_deep_run_resolution.py tests/test_deep_selection.py tests/test_semgrep_normalization.py` (`31 passed`), `python -m qa_z deep --help`, and `python -m ruff check`.
- 2026-04-23: L16 closed run-summary facade hardening by replacing `run_summary.py` importlib re-exports with explicit imports plus `__all__`, then widening verification across run-summary schema and deep-run attachment consumers; verification closed with `python -m pytest -q tests/test_run_summary_architecture.py tests/test_artifact_schema.py tests/test_deep_run_resolution.py` (`22 passed`), `python -m qa_z fast --help`, `python -m qa_z deep --help`, and `python -m ruff check`.
- 2026-04-23: L15 checkpoint closed review/repair reporter packet hardening by replacing `repair_prompt.py` and `review_packet.py` importlib facades with cycle-safe explicit imports plus `__all__`, splitting the old giant `tests/test_repair_prompt.py` into prompt/contracts/review runtime packs with shared support, and adding a same-process `review` then `repair-prompt` smoke to catch import-topology regressions; verification closed with an independent reviewer pass plus `python -m pytest -q tests/test_deep_context_architecture.py tests/test_deep_context_helper_architecture.py tests/test_github_summary_architecture.py tests/test_github_summary_render.py tests/test_github_summary_session.py tests/test_github_summary_deep.py tests/test_repair_prompt.py tests/test_repair_prompt_contracts.py tests/test_repair_prompt_architecture.py tests/test_repair_prompt_helper_architecture.py tests/test_review_packet_architecture.py tests/test_review_packet_runtime.py tests/test_review_commands.py tests/test_cli.py tests/test_current_truth.py` (`140 passed`), `python -m qa_z review --help`, `python -m qa_z repair-prompt --help`, `python -m qa_z github-summary --help`, and `python -m ruff check`.
- 2026-04-23: L14 closed deep-context seam consistency by replacing `deep_context.py` importlib facades with explicit imports plus `__all__`, moving `deep_context_loading.py` to a type-only `DeepContext` import to avoid wrapper cycles, and locking the surface with a no-importlib architecture guard; verification closed with `python -m pytest -q tests/test_deep_context_architecture.py tests/test_deep_context_helper_architecture.py tests/test_github_summary_architecture.py tests/test_github_summary_render.py tests/test_github_summary_session.py tests/test_github_summary_deep.py tests/test_repair_prompt.py tests/test_repair_prompt_architecture.py tests/test_repair_prompt_helper_architecture.py tests/test_review_packet_architecture.py tests/test_review_commands.py tests/test_cli.py` (`105 passed`), `python -m qa_z review --help`, `python -m qa_z repair-prompt --help`, and `python -m ruff check`.
- 2026-04-23: L13 closed GitHub-summary reporter seam consistency by replacing `github_summary.py` importlib facades with explicit imports plus `__all__`, splitting the old monolithic `tests/test_github_summary.py` into render/session/deep packs with shared support, and locking the split with no-importlib plus test-budget architecture guards; verification closed with `python -m pytest -q tests/test_github_summary_architecture.py tests/test_github_summary_render.py tests/test_github_summary_session.py tests/test_github_summary_deep.py tests/test_cli.py tests/test_current_truth.py tests/test_github_workflow.py` (`92 passed`), `python -m qa_z github-summary --help`, and `python -m ruff check`.
- 2026-04-23: L12 closed verification-publish surface cleanup by moving publish dataclasses into `verification_publish_models.py`, replacing the last `verification_publish.py` importlib facade with explicit imports, splitting the oversized publish regression pack into summary/session tests plus shared support, and locking the split with no-importlib plus test-budget architecture guards; verification closed with `python -m pytest -q tests/test_verification_publish_summary.py tests/test_verification_publish_session.py tests/test_verification_publish_architecture.py tests/test_verification_publish_helper_architecture.py` (`21 passed`) and `python -m ruff check`.
- 2026-04-23: L11 closed verification helper façade consistency by replacing `verification_artifacts.py` and `verification_outcome.py` importlib re-export facades with explicit import surfaces plus `__all__` contracts, then locked the rule with dedicated architecture guards and `python -m pytest -q tests/test_verification_architecture.py tests/test_verification_artifact_architecture.py tests/test_verification_outcome_architecture.py tests/test_verification_outcome_helpers.py tests/test_verification_artifact_io.py` (`18 passed`) plus `python -m ruff check`.
- 2026-04-23: L10 checkpoint closed explicit-policy cleanup prioritization by making `runtime_artifact_cleanup_gap` outrank `artifact_hygiene_gap` when policy-managed runtime artifacts still exist, specializing the human cleanup hint plus autonomy cleanup packet toward clear-before-integrate follow-through, and syncing README/schema/current-state wording with `python -m pytest -q tests/test_self_improvement_live_repository.py tests/test_self_improvement_selection_history.py tests/test_self_improvement_selection_output.py tests/test_autonomy.py`, `python -m qa_z self-inspect --json`, `python -m qa_z select-next`, and `python -m ruff check .`.
- 2026-04-23: L9 closed generated-policy false-positive cleanup drift by keeping live runtime-artifact cleanup and artifact-hygiene candidates open while suppressing `evidence_freshness_gap` once the ignore rules plus policy doc are already explicit, then synced README/schema/current-state wording with `python -m pytest -q tests/test_self_improvement_live_repository.py tests/test_self_improvement.py -k "artifact_hygiene or policy_gap or explicit_policy or runtime_artifact_cleanup_from_live_paths"` (`6 passed`), `python -m qa_z self-inspect --json`, and `python -m ruff check .`.
- 2026-04-23: L8 closed the fallback-diversity package by keeping `select-next` on a non-cleanup family after `autonomy_selection_gap-repeated-fallback-cleanup` when alternatives exist, specializing the human `improve_fallback_diversity` action hint from loop-history evidence, and syncing README/schema/current-state wording with `python -m pytest -q tests/test_self_improvement.py tests/test_self_improvement_selection.py tests/test_self_improvement_selection_history.py tests/test_self_improvement_selection_output.py tests/test_autonomy.py tests/test_cli.py tests/test_current_truth.py` (`150 passed`), `python -m qa_z backlog`, and `python -m ruff check .`.
- 2026-04-23: L7 turned runtime-artifact hygiene into an operator-facing cleanup capability by adding `scripts/runtime_artifact_cleanup.py` plus support tests, wiring selection and autonomy cleanup hints to the deterministic review/apply commands, carrying `scripts/runtime_artifact_cleanup.py` through cleanup `context_paths`, and updating generated-versus-frozen policy docs plus worktree triage guidance; verification closed with `python -m pytest -q tests/test_runtime_artifact_cleanup.py tests/test_runtime_artifact_cleanup_architecture.py tests/test_self_improvement_selection_output.py tests/test_autonomy.py tests/test_cli.py tests/test_current_truth.py tests/test_worktree_commit_plan.py` (`168 passed`).
- 2026-04-23: L6 deepened blocked mixed-history dry-run diagnostics so `executor_dry_run_blocked_mixed_history_operator_actions` keeps verification blockers primary while its operator summary still says validation conflicts and retry pressure still need review, then closed the package with `python -m pytest -q tests/test_executor_dry_run_logic.py tests/test_executor_result_dry_run.py tests/test_benchmark_executor_runtime_operator_actions.py tests/test_benchmark_corpus.py tests/test_current_truth.py` (`63 passed`), `python -m qa_z benchmark --fixture executor_dry_run_blocked_mixed_history_operator_actions --json --results-dir benchmarks/results-l6-blocked-mixed` (`1/1 fixtures, overall_rate 1.0`), and `python -m ruff check .`.
- 2026-04-23: L5 checkpoint hardened blocked-loop provenance by adding exact blocked chain loop ids to `loop_health`, taskless human surfaces, and loop-history candidate summaries, then closed the package with an independent reviewer pass plus `python -m pytest -q tests/test_autonomy.py tests/test_loop_health_signal_inputs.py tests/test_self_improvement_selection_history.py tests/test_current_truth.py` (`73 passed`) and `python -m ruff check .`.
- 2026-04-23: L4 hardened taskless-loop readability by adding blocked no-candidate chain residue to `loop_health`, human status, saved loop plans, and current-truth docs, so each blocked loop now records how close it is to the repeated-blocked stop rule; verification closed with `python -m pytest -q tests/test_autonomy.py tests/test_current_truth.py` (`65 passed`) and `python -m ruff check .`.
- 2026-04-23: L3 hardened autonomy action-context bundles by merging loop-local self-inspection into loop-health and repair-session `context_paths`, updating executor-bridge benchmark expectations plus current-truth docs so the same selection context survives from autonomy into bridge-local inputs; verification closed with `python -m pytest -q tests/test_autonomy.py tests/test_executor_bridge.py tests/test_benchmark_corpus.py tests/test_current_truth.py` (`90 passed`), `python -m qa_z benchmark --fixture executor_bridge_action_context_inputs --json --results-dir benchmarks/results-l3-bridge-context` (`1/1 fixtures, overall_rate 1.0`), and `python -m ruff check .`.
- 2026-04-23: L1 restored root Python gate reliability by packaging `tests/` for CLI pytest collection, adding `tests/test_fast_gate_environment.py`, fixing the `alpha_release_preflight` wrapper seam, normalizing AST architecture typing through shared test support, and clearing the full `ruff format/check`, `mypy src tests`, `pytest -q`, and `qa_z fast --selection smart --json` baseline (`1025 passed`).
- 2026-04-23: L2 hardened executor dry-run mixed retry ordering by making repeated rejected results stay ahead of partial retry review in logic, benchmark fixtures, runtime regression tests, and current-truth docs while preserving the same committed corpus size; verification closed with targeted dry-run regression, `python -m pytest -q` (`1028 passed`), `python -m ruff check .`, and `python -m qa_z benchmark --json --results-dir benchmarks/results-l2-full` (`52/52 fixtures, overall_rate 1.0`).
- 2026-04-22: L1 current-truth refresh in progress for release/report surfaces, using fresh `pytest -q`, `ruff check .`, `qa_z --help`, and isolated benchmark evidence as the source of truth.
- 2026-04-22: L2 split release-facing current-truth coverage into `tests/test_current_truth_release_surfaces.py` and added `tests/test_current_truth_architecture.py` so the main file stays within a smaller maintenance budget.
- 2026-04-22: L3 split alpha-release preflight remote/origin contracts into `tests/test_alpha_release_preflight_remote.py` and added `tests/test_alpha_release_preflight_architecture.py` so the main file can focus on CLI/rendering behavior.
- 2026-04-22: L4 split alpha-release preflight CLI, carry-forward probe, and human-render coverage into `tests/test_alpha_release_preflight_cli_render.py`; the main file now keeps only the post-split edge cases.
- 2026-04-22: L5 split `scripts/alpha_release_gate.py` into an orchestration surface plus `scripts/alpha_release_gate_evidence.py`, split release-evidence and human-render tests into dedicated packs, and added architecture guards so evidence/render logic cannot silently collapse back into the monolith.
- 2026-04-22: L6 split `scripts/alpha_release_preflight.py` into runtime plus `scripts/alpha_release_preflight_evidence.py`, moved duplicated preflight test fixtures into `tests/alpha_release_preflight_test_support.py`, and locked the new seam with runtime/support architecture checks.
- 2026-04-22: L7 split `scripts/worktree_commit_plan.py` into a thin runtime plus `scripts/worktree_commit_plan_support.py`, restored batch/owner contracts in the helper seam, moved loader fixtures into `tests/worktree_commit_plan_test_support.py`, and added architecture checks so the commit-plan logic stays out of the CLI wrapper.
- 2026-04-22: L8 split `scripts/alpha_release_bundle_manifest.py` and `scripts/alpha_release_artifact_smoke.py` into thin runtime wrappers plus dedicated support modules, moved loader fixtures into release-script test support helpers, and added architecture guards so bundle/install-smoke logic stays out of the CLI surface.
- 2026-04-22: L9 replaced `importlib` re-export façades in `src/qa_z/verification.py` and `src/qa_z/benchmark.py` with explicit import surfaces, added explicit public `__all__` contracts, and tightened architecture checks so those entry modules stay readable without lazy import indirection.
- 2026-04-22: L10 replaced `importlib` façade re-exports across `src/qa_z/executor_bridge.py`, `src/qa_z/executor_result.py`, `src/qa_z/repair_session.py`, `src/qa_z/executor_history.py`, and `src/qa_z/executor_ingest.py` with explicit import surfaces, added public `__all__` contracts, and tightened architecture guards so executor return-path seams stay readable without lazy import indirection.
- deep run lineage is now explicit through `run_resolution`, and benchmark fixture expectations pin attached versus standalone deep evidence
- benchmark `--results-dir` writes are guarded by `.benchmark.lock`, so parallel fixture experiments should use distinct results directories
- release evidence freshness and consistency diagnostics now include JSON `generated_at`, human `Generated at:` output, `release_evidence_consistency` next actions, `ready_for_remote_checks`, `actual_origin_target`, `remote_ref_count`, `remote_ref_sample`, and self-inspection `release_evidence_count`; the remaining release risk is remote publication, not local evidence shape
- the remaining active roadmap is now about operator diagnostics depth, evidence-policy maintenance, loop-health clarity, selective benchmark breadth, and report/template maintenance rather than missing baseline return-path foundations

## Ordering Principle

The numbered sections below are a standing gap map, not a required execution
order. With the documented L29 Priority 5 mixed-history slice now landed, the
next execution order is:

1. preserve the generated versus frozen evidence policy as artifact surfaces evolve
2. maintain loop-health summary clarity as autonomy surfaces grow
3. deepen operator diagnostics around executor history only where a new deterministic dry-run slice adds unique evidence without live execution
4. expand benchmark realism only where current coverage is still thin and a new deterministic slice adds unique evidence
5. keep report, template, and example current-truth sync as maintenance so it does not drift back

## Priority 1: Empty-Loop And Loop-Health Maintenance

### Why This Matters

Autonomy already has backlog reseeding, fallback-family rotation, blocked-loop
stopping rules, explicit `loop_health` summaries, and repeated-blocked
`stop_reason` evidence. The remaining work is to keep that explanation layer
current as new loop states are added.

### Repository Evidence

- `.qa-z/loops/history.jsonl` can accumulate recent empty-loop chains
- `outcome.json`, `loop_plan.md`, history, and `qa-z autonomy status` now
  preserve `selection_gap_reason`, backlog-open counts, `loop_health`, and
  stale-open-item closure counts for taskless loops
- `src/qa_z/autonomy.py` and `src/qa_z/self_improvement.py` already classify
  blocked and fallback loops, so the remaining work is refinement rather than a
  missing subsystem
- `tests/test_autonomy.py` and `tests/test_self_improvement.py` already cover
  the current empty-loop and reseeding behavior

### Scope

- keep empty-loop classification, blocked-chain residue, exact blocked loop provenance, and stop conditions explicit
- keep loop history readable when no task is selected
- preserve additive `loop_health` fields as autonomy evolves
- keep selected fallback families visible in status and loop plans for repeated-selection diagnostics
- keep loop-health prepared action `context_paths` pointing at loop-history evidence together with loop-local self-inspection and report context for fallback-diversity work
- keep autonomy-created repair-session action `context_paths` carrying loop-local self-inspection plus selected verification evidence into executor bridge `inputs/context/` files

## Priority 2: Generated Versus Frozen Evidence Policy Maintenance

### Why This Matters

QA-Z now produces valuable runtime evidence, and the boundary between generated
local artifacts and intentionally tracked repository evidence is explicit in
`docs/generated-vs-frozen-evidence-policy.md`. The remaining need is to keep new
artifact surfaces aligned with that policy.

### Repository Evidence

- `benchmarks/results/summary.json` and `benchmarks/results/report.md`
- `.qa-z/**` runtime state and loop artifacts
- `docs/generated-vs-frozen-evidence-policy.md`
- `.gitignore`
- self-inspection artifact-hygiene and evidence-freshness policy snapshot fields

### Scope

- keep root `.qa-z/**` and `benchmarks/results/work/**` local
- keep `benchmarks/results/summary.json` and `benchmarks/results/report.md` local by default unless intentionally frozen evidence is documented
- keep `python scripts/runtime_artifact_cleanup.py --json` and `--apply --json` aligned with the policy-managed roots and tracked-file skip behavior
- update the policy document and self-inspection terms when new artifact surfaces are added
- keep `triage_and_isolate_changes` prepared actions carrying `docs/generated-vs-frozen-evidence-policy.md` through `context_paths`
- keep loop-local self-inspection continuity across cleanup and workflow prepared actions so later bridge or release handoff surfaces do not depend on mutable `latest` pointers
- preserve the live git or runtime artifact evidence gate so stale report-only text does not reseed closed deferred-cleanup, commit-isolation, or integration work

## Priority 3: Mixed-Surface Benchmark Breadth

### Why This Matters

Executed mixed-surface realism is now landed. The first mixed fast plus deep executed fixture, `mixed_fast_deep_handoff_dual_surface`, is landed. The second mixed fast plus deep executed fixture, `mixed_fast_deep_handoff_ts_lint_python_deep`, is also landed. The third mixed fast plus deep executed fixture, `mixed_fast_deep_handoff_py_lint_ts_test_dual_deep`, is landed with Python lint, TypeScript test, and dual deep evidence. The fourth mixed fast plus deep executed fixture, `mixed_fast_deep_scan_warning_fast_only`, pins the case where deep contributes non-blocking multi-source scan warnings but repair handoff must stay fast-only. Executor bridge action-context packaging is now pinned by `executor_bridge_action_context_inputs`, and missing action-context guide/stdout diagnostics are pinned by `executor_bridge_missing_action_context_inputs`. Executor-result ingest stdout diagnostics are now pinned through source context, freshness/provenance, and backlog implication checks. Semgrep scan-quality warning visibility is now pinned by both `deep_scan_warning_diagnostics` and `deep_scan_warning_multi_source_diagnostics` through `scan_warning_count`, `scan_warnings`, and summary-level `scan_quality`; deep run lineage is now pinned through `run_resolution_source`, `attached_to_fast_run`, and `run_resolution_fast_summary_path`. The corpus still has room to broaden without redesigning its contract.

### Repository Evidence

- `docs/benchmarking.md`
- `tests/test_benchmark.py`
- committed fixtures under `benchmarks/fixtures/`

### Scope

- add more mixed fast plus deep executed combinations beyond the first three executed fixtures
- add denser mixed repair handoff aggregation cases
- keep `executor_bridge_action_context_inputs` and `executor_bridge_missing_action_context_inputs` passing as bridge-local action context copying and missing-context diagnostics evolve
- keep deep `run_resolution` expectations in attached and standalone fixtures so artifact lineage remains deterministic
- use distinct benchmark `--results-dir` values for parallel fixture experiments; the default results directory is intentionally single-writer locked
- add more deterministic dry-run history combinations where they increase realism

## Priority 4: Report, Template, And Example Sync

### Why This Matters

The main README and schema docs have moved closer to current truth. A
template/example sync first pass now also keeps the public config example,
README excerpt wording, downstream agent templates, and placeholder examples
honest about the current alpha surface. A workflow template live-free gate sync
pass now pins the repository workflow and workflow template as deterministic CI
gates that preserve artifacts before applying fast/deep verdicts and do not
call live executors, ingest executor results, perform autonomous repair, mutate
branches, commit, push, or post bot comments. A TypeScript demo live-free
boundary sync pass now also pins the runnable TypeScript example as a fast-only
demo, not a Next.js demo, TypeScript-specific deep automation example, or live
executor workflow. A FastAPI demo deterministic boundary sync pass now pins the
runnable Python example as a dependency-light deterministic fast and
repair-prompt demo, not a mandatory web-server, deep automation,
repair-session, executor bridge, executor-result, or live-agent workflow. A
Next.js placeholder live-free boundary sync pass now pins
`examples/nextjs-demo` as placeholder-only and non-runnable until real
package/config/source/test files exist, with no live-agent call and no executor
bridge/result workflow. A benchmark report generated-output policy sync pass
now makes `report.md` repeat its local generated-artifact policy and label
category rows as covered or not covered for selected runs. Report-style docs,
workflow templates, and examples still need regular sync so they do not keep
reseeding stale backlog items.

### Repository Evidence

- `docs/reports/current-state-analysis.md`
- `docs/reports/next-improvement-roadmap.md`
- `qa-z.yaml.example`
- workflow templates under `templates/`

### Scope

- keep reports aligned with landed executor-history, benchmark, and template/example surfaces
- keep examples and templates aligned with actual CLI/runtime behavior as it evolves
- avoid stale report language that reopens already-landed work

## Priority 5: Executor Operator Diagnostics

### Why This Matters

The live-free safety dry-run exists, its provenance is clearer, and an operator diagnostics first pass now carries operator decision, operator summary and recommended actions through dry-run, repair-session, publish, and benchmark surfaces. Repair-session outcome Markdown and GitHub-facing publish summaries now also keep ordered `Action <id>:` lines for those recommended actions instead of flattening mixed-history residue into one sentence. Together, all committed dry-run fixtures now pin operator decision, operator summary, and recommended action residue, including `executor_dry_run_validation_noop_operator_actions`, `executor_dry_run_repeated_rejected_operator_actions`, `executor_dry_run_validation_conflict_repeated_rejected_operator_actions`, `executor_dry_run_repeated_noop_operator_actions`, `executor_dry_run_blocked_mixed_history_operator_actions`, `executor_dry_run_empty_history_operator_actions`, `executor_dry_run_scope_validation_operator_actions`, `executor_dry_run_missing_noop_explanation_operator_actions`, and `executor_dry_run_mixed_attention_operator_actions`. Attention fixtures now also keep top-level next recommendations aligned with their specific operator actions, empty-history sessions mark `executor_history_recorded` as attention, and repeated no-op history keeps rule counts aligned by marking `retry_boundary_is_manual` as attention. Complete dry-run rule buckets are now pinned across the committed fixtures as well, so rule partition drift is treated as a benchmark regression. The repeated rejected fixture now keeps rejected-result inspection ahead of partial retry review when mixed partial and rejected histories repeat, the validation-conflict mixed retry slice keeps validation-conflict review primary while still preserving rejected-result inspection and partial retry review in non-blocked history, the blocked mixed-history depth pass keeps verification blockers primary while still saying validation conflicts and retry pressure still need review, and the mixed-attention depth pass keeps non-blocked histories explaining combined validation conflict, no-op explanation, and retry pressure in the operator summary. The formerly documented next slice is now committed as `executor_dry_run_validation_conflict_repeated_rejected_operator_actions`; remaining Priority 5 work is selective maintenance when future history combinations add genuinely new operator residue.

### Repository Evidence

- `src/qa_z/executor_dry_run.py`
- `src/qa_z/executor_dry_run_logic.py`
- `src/qa_z/reporters/verification_publish.py`
- current dry-run fixtures and self-inspection history candidates

### Scope

- enrich deterministic explanations for retry, pause, and escalation decisions
- keep history-aware diagnostics readable without adding orchestration
- keep the committed validation-conflict plus repeated rejected retry case aligned across logic, benchmark, publish, and continuity surfaces, and add new dry-run combinations only when they contribute unique operator residue
- preserve the live-free safety boundary

## Deferred Or Optional After These Priorities

These should stay deferred until the current baseline remains stable and the
remaining sync work is complete:

- live Codex or Claude execution
- automatic candidate reruns after external repair
- remote orchestration, queues, schedulers, or daemons
- standalone GitHub annotation/reporting surfaces beyond the current summary and
  SARIF path
- new deep engines beyond the current Semgrep-backed implementation

## Immediate Execution Rule

Do not start a fresh feature line before the current worktree is split into the
existing commit batches and the full validation baseline is rerun. The current
repository has enough value already that preserving a clean, explainable baseline
is worth more than adding one more unsplit feature. The alpha closure readiness snapshot
in `docs/reports/worktree-commit-plan.md` is the current gate reference
for that split, and self-inspection now carries that snapshot as closure-aware
commit-isolation evidence. Compact selected-task evidence summaries now
prioritize that closure-aware evidence for human `qa-z select-next` output, and
the selected-task human surfaces now add deterministic action hints for closure
recommendations without changing the live-free boundary. Plain `qa-z backlog`
output now mirrors the same hints for active items while `--json` stays stable.
Plain `qa-z self-inspect` now carries matching top-candidate summaries, so the
planning path is readable from inspection through selection without adding live
execution. These human planning surfaces and generated loop plans now also carry
deterministic `validation:` command hints, making the evidence-refresh step
visible without changing the JSON contract. Dirty-worktree evidence now also
summarizes modified and untracked paths by deterministic repository area, so the
remaining commit-isolation work can start from benchmark/docs/source/test counts
instead of a raw path dump. The corresponding dirty-worktree action hint now
uses those top areas when present, so selection and loop-plan handoff text points
at the same first triage surface as the evidence.
Commit-isolation handoff now follows the same pattern: the candidate keeps the
closure plan as its primary compact evidence, but its full evidence and action
hint can reuse dirty area counts to make the foundation split target clearer.
The compact human summary now appends an `action basis:` suffix when those area
counts explain the action hint, preserving the closure snapshot as the primary
evidence while keeping the area-specific handoff readable.
Benchmark result snapshot directories such as `benchmarks/results-*` now fall
under generated runtime artifact handling, so remaining alpha closure cleanup can
separate local-only outputs from intentional frozen evidence before commit
splitting.
Generated cleanup handoff now mirrors the area-basis pattern: compact human
evidence can append `action basis:` with `generated_outputs` or
`runtime_artifacts` evidence when the primary compact line is report context.
The latest compact strict helper snapshot now makes that split explicit: the
visible dirty tree has `0` generated roots and no source-ownership drift, while
the include-ignored policy snapshot still shows `7` ignored generated roots,
split into `0` local-only runtime artifact roots and `7` local-by-default
benchmark evidence roots. That means the remaining cleanup work is policy
review, patch-add ownership for cross-cutting surfaces, and continuity
maintenance, not source-path ownership repair.
Repeated fallback recovery now also hardens the planning path itself: when a
repeated cleanup-family loop-health gap competes with concrete non-loop-health
alternatives, the initial batch selection deprioritizes that meta-gap and can
suppress the repeated family long enough for a real remediation candidate to
surface first. A one-loop autonomy run now confirms that behavior end to end by
leaving `qa-z self-inspect` with only the concrete worktree integration tasks
open instead of reintroducing `autonomy_selection_gap-repeated-fallback-cleanup`.

