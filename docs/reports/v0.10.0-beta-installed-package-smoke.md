# QA-Z v0.10.0-beta Installed-Package Smoke

Status: local installed-package runtime smoke passed. This report records local
wheel and sdist behavior only; it does not publish to PyPI or TestPyPI.

## Current Truth

- Source metadata under test: `qa-z` / `0.10.0b0`.
- Production PyPI has not been published.
- No TestPyPI upload was run for `0.10.0b0`.
- No `twine upload`, tag, GitHub Release, deploy, or live PyPI install claim
  occurred in this smoke.
- Built artifacts were created in an isolated temporary source copy, not in the
  repository worktree.
- Generated demo and verify artifacts were created under temporary smoke
  workspaces, not under repository source paths.

## Command

```bash
python scripts/installed_package_smoke.py --json
```

## Result

- Summary: `installed package smoke passed`.
- Exit code: `0`.
- `registry_upload_executed=false`.
- Wheel artifact smoke: `PASS`.
- Sdist artifact smoke: `PASS`.

## Matrix

Both the locally built wheel and locally built sdist passed the same installed
runtime matrix:

1. Install artifact into a fresh virtual environment.
2. Run `qa-z --help`.
3. Run `python -m qa_z --help`.
4. Run `qa-z demo auth-bug --json`.
5. Run `qa-z doctor --json --path <demo-root> --config qa-z.demo.yaml`.
6. Run `qa-z guard --from-run latest --adapter codex`.
7. Run `qa-z repair-prompt --from-run latest --adapter codex`.
8. Copy `app/auth.fixed.py` over `app/auth.py`.
9. Run `qa-z verify --from-run latest --json`.

`qa-z verify` returned exit code `0` with verdict `improved` after the deterministic
fixed-file copy. The installed-package smoke treats that as PASS because the
command produced post-repair verification evidence from installed wheel and
sdist environments instead of crashing or missing packaged resources.

## Evidence Notes

- Wheel name observed: `qa_z-0.10.0b0-py3-none-any.whl`.
- Sdist name observed: `qa_z-0.10.0b0.tar.gz`.
- Demo resource loading passed from both installed artifact forms.
- CLI entrypoint and module entrypoint both passed from both artifact forms.
- The smoke script copies source to a temporary build directory before running
  `python -m build --sdist --wheel`, so repository `dist/`, `build/`,
  `src/qa_z.egg-info/`, `.qa-z/`, and cache directories are not produced as
  source worktree changes.

## Explicit Non-Actions

- No production PyPI upload occurred.
- No TestPyPI upload occurred.
- No registry credentials were loaded or required.
- No tag or GitHub Release was created.
- No deploy occurred.
- No live `pipx install qa-z` or `uv tool install qa-z` claim is made.
- No generated package, build, cache, or `.qa-z` artifact should be committed.
