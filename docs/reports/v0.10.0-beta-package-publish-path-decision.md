# QA-Z v0.10.0-beta Package Publish Path Decision Packet

Packet date: 2026-05-18
Source basis: clean `origin/main` at `dc23cdee8be1db846bbf17b1d562a91bf9bce349`
Decision type: release-owner package publish path decision packet, not release
execution.

Current decision: `No release yet`.
Selected next path: TestPyPI rehearsal.

`v0.10.0-beta` is not released. Release execution remains `NO-GO`. This packet
separates the release-owner path choice from any tag, GitHub Release, package
upload, deploy, version metadata change, registry credential use, or live install
claim.

## Purpose

The purpose of this packet is to give the release owner one decision surface for
the package publish path after the no-upload package smoke passed. It does not
publish to TestPyPI or PyPI. It does not approve package publishing. It only
records the current state, available options, required approvals, and proof that
would be required before a later execution packet could run.

This packet keeps the README first-screen growth slice out of scope. README
local changes from the dirty root checkout are not part of this decision packet.

## Current State

- `v0.10.0-beta` is not released.
- Current public alpha remains `v0.9.9-alpha`.
- Current package metadata remains `0.9.8a0`.
- Current active install remains GitHub source/tag install until package publish
  is approved, executed, and proven.
- Tool-equipped no-upload tool smoke passed in
  `docs/reports/v0.10.0-beta-tool-smoke-execution.md`.
- The no-upload tool smoke passed, but it does not prove package publish,
  registry availability, tag creation, GitHub Release creation, deploy, version
  execution, or PyPI live install availability.
- `registry_upload_executed=false`.
- Release execution remains `NO-GO`.
- Current release-owner decision remains `No release yet`.
- Registry credentials are not recorded here. Credential presence is not
  approval.

Current source links:

- No-release decision:
  `docs/reports/v0.10.0-beta-no-release-decision.md`
- Release execution checklist:
  `docs/reports/v0.10.0-beta-release-execution-checklist.md`
- Version policy: `docs/reports/v0.10.0-beta-version-policy.md`
- Tool-smoke execution:
  `docs/reports/v0.10.0-beta-tool-smoke-execution.md`
- Rollback/yank policy:
  `docs/reports/v0.10.0-beta-rollback-yank-policy.md`
- Package publish plan: `docs/package-publish-plan.md`
- TestPyPI rehearsal approval:
  `docs/reports/v0.10.0-beta-testpypi-rehearsal-approval.md`
- TestPyPI rehearsal execution packet:
  `docs/reports/v0.10.0-beta-testpypi-rehearsal-execution-packet.md`
- TestPyPI rehearsal GO/NO-GO packet:
  `docs/reports/v0.10.0-beta-testpypi-rehearsal-go-no-go.md`

## Decision Options

The release owner must select exactly one explicit path before any
release-adjacent execution packet can run:

- `No release yet`
- `TestPyPI rehearsal only`
- `TestPyPI publish`
- `PyPI beta publish`
- `metadata-only version PR`
- `GitHub prerelease only`

Any option other than `No release yet` still requires a separate approved packet
with proof, approval fields, and explicit non-actions before execution.

## Option Comparison

| Option | What it means | Current status | Execution allowed now |
| --- | --- | --- | --- |
| `No release yet` | Keep the current no-release decision and leave all publish paths blocked. | Current recommendation | No |
| `TestPyPI rehearsal only` | Run local package build or no-upload rehearsal evidence without registry upload. | Local no-upload proof exists; refresh only if artifacts change. | No registry execution |
| `TestPyPI publish` | Upload a selected package metadata version to TestPyPI. | `BLOCKED` on approval, credentials, version decision, final proof, and rollback/yank policy. | No |
| `PyPI beta publish` | Publish a beta package so `pipx install qa-z` can become the target public install path after proof. | `BLOCKED` on approval, credentials, version decision, final proof, and rollback/yank policy. | No |
| `metadata-only version PR` | Change package metadata such as `0.10.0b0` in a separate approved PR without uploading. | `BLOCKED` until release owner approves metadata scope. | No |
| `GitHub prerelease only` | Create a GitHub prerelease/tag without package registry upload. | `BLOCKED` on approval, exact SHA proof, tag/release policy, and rollback policy. | No |

## Required Approval Fields

A later path-specific packet must record these fields before any selected path
executes:

| Field | Required for | Current state |
| --- | --- | --- |
| `RELEASE_EXECUTION_APPROVED` | Any tag, GitHub Release, package publish, deploy, or version execution. | `NOT APPROVED` |
| `PACKAGE_PUBLISH_ALLOWED` | Any TestPyPI, PyPI, npm, GitHub Packages, or package registry upload. | `NOT APPROVED` |
| release owner | Any release path decision other than this current no-release state. | `NOT APPROVED` |
| selected path | Any release-adjacent action. | `No release yet` |
| target package metadata version | TestPyPI, PyPI, metadata-only PR, or package artifacts. | `BLOCKED`; current metadata remains `0.9.8a0` |
| target registry | TestPyPI, PyPI, npm, GitHub Packages, or another registry. | `BLOCKED` |
| exact release SHA | Any tag, release, package publish, or metadata execution candidate. | `BLOCKED` until SHA freeze |
| remote CI proof link | Any execution candidate. | `BLOCKED` until final proof packet |
| public raw proof link | Any execution candidate. | `BLOCKED` until final proof packet |
| advisory decision proof link | Any path that treats the Next.js/PostCSS advisory as resolved, deferred, or accepted. | `BLOCKED` |
| rollback/yank policy link | Any tag, GitHub Release, deploy, or registry publish path. | `BLOCKED` |
| credential confirmation | Any registry upload path. | `BLOCKED`; credentials must not be printed |

Credential presence is not approval.

## Required Proof By Option

| Option | Required proof before execution |
| --- | --- |
| `No release yet` | This packet plus the existing no-release decision are enough to keep execution blocked. |
| `TestPyPI rehearsal only` | Fresh local build artifacts, no-upload smoke output, `registry_upload_executed=false`, generated artifact cleanup, and explicit no-upload confirmation. |
| `TestPyPI publish` | Release-owner approval, selected PEP 440 metadata version, exact frozen SHA, CI proof, public raw proof, TestPyPI credential confirmation without secrets, TestPyPI package URL/version proof, and rollback/yank policy. |
| `PyPI beta publish` | Release-owner approval, selected public beta metadata version, exact frozen SHA, CI proof, public raw proof, PyPI credential confirmation without secrets, PyPI package URL/version proof, install proof, and rollback/yank policy. |
| `metadata-only version PR` | Release-owner approval for metadata scope, tests/docs proving the metadata change, and explicit proof that no upload, tag, release, deploy, or live install claim occurred. |
| `GitHub prerelease only` | Release-owner approval, exact frozen SHA, CI proof, public raw proof, tag/release notes policy, GitHub prerelease URL proof, and tag/release rollback policy. |

No-upload package smoke is prerequisite evidence only. It is not publish proof.

## Install Path Impact

Current active install remains GitHub source/tag install until a later approved
PyPI path is executed and proven.

Future PyPI-published target commands, not current live install claims:

```bash
pipx install qa-z
uv tool install qa-z
```

Do not present these commands as live until PyPI publish is approved, executed,
and proven with the selected package metadata version and package URL.

## Blocked Commands

These examples are blocked commands, not safe commands. They must stay out of
ordinary validation sections until the release owner records all required
approval, proof, version, credential, advisory, rollback/yank, and cleanup
fields.

```bash
python -m twine upload --repository testpypi dist/*
python -m twine upload dist/*
git tag ...
gh release create ...
deploy commands
```

Blocked actions also include `uv publish`, npm registry publish, GitHub Packages
publish, tag creation, GitHub Release creation, deployment, GitHub settings
mutation, bot comments, live model calls, changing `pyproject.toml`, bumping
version metadata, and loading registry credentials for this decision-only
packet.

## Recommended Current Decision

Recommended current decision: keep `No release yet` for release execution while
using TestPyPI rehearsal as the selected next review path.

The next release-owner action is to prepare a non-upload TestPyPI rehearsal
execution packet. The approval packet is
`docs/reports/v0.10.0-beta-testpypi-rehearsal-approval.md`; it does not
authorize upload. The TestPyPI rehearsal execution packet is
`docs/reports/v0.10.0-beta-testpypi-rehearsal-execution-packet.md`; it does not
upload. Until execution approval and proof exist, release execution remains
`NO-GO`, package publish remains blocked, `pyproject.toml` remains unchanged,
and `pipx install qa-z` remains a future target rather than a live claim.

## Explicit Non-actions

This packet did not:

- publish to TestPyPI or PyPI;
- run `twine upload`;
- create a tag;
- create a GitHub Release;
- deploy;
- change `pyproject.toml`;
- bump version metadata;
- load registry credentials;
- mutate GitHub settings;
- post bot comments;
- call live model APIs;
- commit generated artifacts;
- claim `pipx install qa-z` is live;
- claim `uv tool install qa-z` is live;
- include README first-screen local changes;
- resolve release-owner approval, credentials, version execution, final SHA
  proof, advisory proof, or rollback/yank blockers.
