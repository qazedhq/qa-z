# QA-Z v0.10.0-beta TestPyPI Rehearsal Upload Proof

Proof date: 2026-05-24
Proof type: TestPyPI-only rehearsal upload evidence, not production PyPI release.

## Purpose

This report records the completed TestPyPI rehearsal upload for QA-Z package
metadata `qa-z` version `0.9.8a0`. It preserves the boundary between TestPyPI
rehearsal proof and production PyPI release proof.

## Upload Result

TestPyPI upload completed.

`registry_upload_executed=true` for TestPyPI only.

## TestPyPI URL

TestPyPI package URL is https://test.pypi.org/project/qa-z/0.9.8a0/

## Uploaded Artifacts

Uploaded artifacts:

- `dist/qa_z-0.9.8a0.tar.gz`
- `dist/qa_z-0.9.8a0-py3-none-any.whl`

These `dist/` paths are generated evidence only. They must not be staged or
committed.

## Install Smoke Result

Fresh virtual environment install from TestPyPI passed.

`python -m qa_z --help` passed after TestPyPI install.

TestPyPI install uses TestPyPI index, not production PyPI.

`pipx install qa-z` is still not a live PyPI install claim.

## Registry Boundary

PyPI upload did not occur.

No twine upload to PyPI occurred.

Production PyPI remains out of scope.

No tag, GitHub Release, deploy, or version bump occurred.

No `pyproject.toml` bump occurred; package metadata remains `qa-z` /
`0.9.8a0` unless a separate approved metadata PR changes it.

## Explicit Non-actions

This proof did not:

- upload to production PyPI;
- run `twine upload` against production PyPI;
- create a tag;
- create a GitHub Release;
- deploy;
- change `pyproject.toml`;
- bump version metadata;
- mutate GitHub settings;
- post bot comments;
- call live model APIs;
- commit generated artifacts;
- claim production PyPI availability;
- add a live `pipx install qa-z` PyPI install claim.

## Security Boundary

Credential values are not included.

QAZ_TOKEN.txt must not be committed.

QAZ_TOKEN.txt must not be staged.

QAZ_TOKEN.txt content must not be printed.

dist artifacts are generated evidence only and must not be committed.

`build/`, `src/qa_z.egg-info/`, `.qa-z/`, cache directories, and generated
artifacts must not be committed.

## Remaining Blockers Before PyPI

- Release owner must explicitly approve production PyPI publish.
- Production PyPI credentials must be confirmed out of band without printing
  secrets.
- Final release-candidate SHA proof must be regenerated if source changes.
- Production PyPI upload, package URL proof, and install smoke proof remain
  absent.
- `pipx install qa-z` must remain future-only until production PyPI publish is
  approved, executed, and proven.
- Tag, GitHub Release, deploy, and metadata-bump decisions remain separate
  release-owner actions.

## Recommended Next Step

Review this TestPyPI-only proof and decide whether a separate production PyPI
beta publish packet should be prepared. Until that decision is approved,
production PyPI remains `NO-GO`.
