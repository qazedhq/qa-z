# QA-Z v0.10.0-beta Readiness Audit

Audit date: 2026-05-15
Audited source: `origin/main` at `9fc11b401089e44da152f7ee911f97b839a0d4ac`
Audit type: release-candidate gap report, not release execution.

## Current Verdict

Current verdict: `NO-GO` for release execution; `PARTIAL` for local
v0.10.0-beta readiness evidence.

QA-Z is still an alpha local deterministic QA control plane. `v0.10.0-beta` is
planned, not released. This audit did not perform package publish, tag
creation, GitHub Release creation, deploy, push, bot comment, live model call,
or external-service mutation.

## Current Release State

- Open GitHub issues: `0` as checked through the GitHub API.
- Open GitHub pull requests: `0` as checked through the GitHub API.
- Last launch/demo blocker checked: issue #4 is closed as completed, and PR #59
  is merged into `origin/main`.
- Current public alpha: `v0.9.9-alpha` GitHub prerelease.
- Historical first public alpha: `v0.9.8-alpha`.
- `v0.10.0-beta`: planned next release target for release polish,
  hosted-demo path, and package-publish readiness.
- Package metadata version: `0.9.8a0` in `pyproject.toml`.
- Package registry publish state: no PyPI, TestPyPI, npm, GitHub Packages, or
  other registry publish is approved or claimed complete.

## Target Mapping

| v0.10.0-beta target | Current evidence | Status |
| --- | --- | --- |
| Release polish | Release docs, launch checklist, package publish plan, worktree commit-plan report, and public current-truth tests exist. | `PARTIAL` |
| Hosted-demo path | `docs/hosted-demo.md`, `docs/docs-site.md`, and hosted-demo tests keep the path static/local replay, not QA-Z Cloud. | `PARTIAL` |
| Package-publish readiness | Local build and artifact smoke tooling exist; registry upload remains approval-gated. | `BLOCKED` |
| GitHub star growth readiness | README, launch package, comparison, docs site, hosted demo, examples, and launch posts are present. | `PARTIAL` |
| Release execution | Requires explicit human release-owner approval and external evidence. | `BLOCKED` |

## Current-Truth Findings

- README and quickstart use GitHub source/tag install paths, not live PyPI
  `pipx install qa-z` or `uv tool install qa-z` claims.
- `docs/package-publish-plan.md` lists `pipx install qa-z` and
  `uv tool install qa-z` only under future `v0.10.0-beta` planning, while the
  active rehearsal remains local-only and records `registry_upload_executed=false`.
- `docs/roadmap.md`, `docs/product/PRODUCT_DIRECTION.md`, and release notes all
  keep `v0.9.9-alpha`, `v0.10.0-beta`, and package metadata `0.9.8a0` separate.
- Hosted-demo docs explicitly say the hosted demo is static replay, not QA-Z
  Cloud, and must not imply live-agent execution or uploaded source analysis.
- The launch checklist still leaves package registry credentials, repository
  settings, and standalone marketplace work as external blockers.
- Next.js/PostCSS advisory decision details are tracked in
  `docs/reports/v0.10.0-beta-nextjs-advisory-decision.md`; the advisory remains
  unresolved until a release-owner option is selected and proven, and release
  execution `NO-GO` remains unchanged.
- Exact SHA proof for current `origin/main` is tracked in
  `docs/reports/v0.10.0-beta-exact-sha-proof.md`; it records remote CI and
  public raw evidence, but it is not release execution and release execution
  `NO-GO` remains unchanged.

## Release Boundaries

Do not change `pyproject.toml` version, create a tag, create a GitHub Release,
publish to any package registry, deploy, push, or mutate GitHub settings from
this readiness audit.

Release execution remains blocked until a human release owner explicitly
approves the relevant action. Package upload additionally requires both:

- `RELEASE_EXECUTION_APPROVED=true`
- `PACKAGE_PUBLISH_ALLOWED=true`

Proof-branch push, tag creation, GitHub Release creation, package upload, docs
site deployment, social preview upload, and repository topic changes are
separate approval-gated actions.

## Validation Matrix

This table records the local audit commands for this report. `NOT RUN` means
the command was not attempted in this environment. `BLOCKED` means the command
requires credentials, tools, approval, or an external service state that this
audit must not fabricate.

| Gate | Command or check | Status | Evidence |
| --- | --- | --- | --- |
| GitHub backlog | GitHub API open issues / PRs | `PASS` | `0` open issues and `0` open PRs. |
| Local status | `git status --short -uall` | `PASS` | Initial isolated worktree was clean; final cleanup must leave only intentional source/doc/test changes. |
| Worktree plan | `python scripts/worktree_commit_plan.py --summary-only --json --fail-on-generated --fail-on-cross-cutting` | `PASS` | `status=ready`, `changed_batch_count=2`, `changed_path_count=6`, no generated, cross-cutting, report, shared patch-add, unassigned, or product-decision blockers after routing the new packet; `docs/agent/work-slice-ledger.md` is classified as approved alpha support. |
| Public docs truth | `python -m pytest tests/test_public_docs_current_truth.py tests/test_beta_readiness_docs.py -q` | `PASS` | `20 passed`. |
| Focused commit-plan routing | `python -m pytest tests/test_worktree_commit_plan_public_docs.py tests/test_worktree_commit_plan_validation_commands.py tests/test_beta_readiness_docs.py -q` | `PASS` | `8 passed`. |
| Full pytest | `python -m pytest -q` | `PASS` | `1804 passed in 100.27s`. |
| Ruff lint | `python -m ruff check .` | `PASS` | Passed after adding the report and commit-plan routing. |
| Ruff format | `python -m ruff format --check .` | `PASS` | Initial check found `tests/test_beta_readiness_docs.py`; `python -m ruff format tests/test_beta_readiness_docs.py` reformatted it, then check passed with `652 files already formatted`. |
| Mypy | `python -m mypy src tests` | `PASS` | No issues found in `565` source files. |
| Text hygiene | `python scripts/check_text_file_hygiene.py --source working-tree --critical-profile public` | `PASS` | `text file hygiene passed`. |
| CLI help | `python -m qa_z --help`; `python -m qa_z doctor --help` | `PASS` | Both help surfaces rendered successfully. |
| Demo smoke | `python -m qa_z demo auth-bug --json` | `PASS` | JSON payload returned `status=created` under `.qa-z/demo/auth-bug`. |
| Benchmark | `python -m qa_z benchmark --results-dir benchmarks/results-ci --json` | `PASS` | `54/54 fixtures`, `overall_rate=1.0`. |
| Package build | `python -m build --sdist --wheel` | `PASS` | Built `qa_z-0.9.8a0.tar.gz` and `qa_z-0.9.8a0-py3-none-any.whl`. |
| Artifact smoke | `python scripts/alpha_release_artifact_smoke.py --json` | `PASS` | Wheel and sdist installed without dependency resolution. |
| Artifact smoke with deps | `python scripts/alpha_release_artifact_smoke.py --with-deps --json` | `PASS` | Wheel and sdist installed with dependency resolution. |
| Twine check | `python -m twine check dist/*` | `NOT RUN` | `python -m twine --version` failed: `No module named twine`. |
| pipx wheel smoke | `pipx run --spec dist/<wheel> qa-z --help` | `NOT RUN` | `pipx --version` failed because `pipx` is not installed on PATH. |
| uvx wheel smoke | `uvx --from dist/<wheel> qa-z --help` | `NOT RUN` | `uvx --version` failed because `uvx` is not installed on PATH. |
| Next.js demo smoke | `npm install`; `npm run lint`; `npm run typecheck`; `npm test`; `python -m qa_z plan --path . --title "Protect Next.js invoice access" --issue issue.md --spec spec.md`; `python -m qa_z fast --path . --selection smart` in `examples/nextjs-demo` | `PASS` | npm `11.6.2` on Node `v25.2.1`; install completed with `2` moderate advisories; lint passed; typecheck passed; Vitest `3 passed`; QA-Z plan created a contract; QA-Z fast passed. |
| Next.js advisory review | temporary lockfile generated outside the repo from `examples/nextjs-demo/package.json`; `npm audit --json --omit=dev`; `npm view next@15.5.18 dependencies --json`; `npm view next@latest version dependencies --json`; `npm view postcss@latest version` | `BLOCKED` | Audit reproduced `2` moderate advisories from `GHSA-qx2v-qp2m-jg93` / `CVE-2026-41305`: requested `next@^15.0.0` resolved to `next@15.5.18`, which depends on `postcss@8.4.31`; advisory affects `postcss <8.5.10`, patched version is `8.5.10`, and current `postcss@latest` is `8.5.14`. `next@latest` resolved to `16.2.6` and still declared `postcss@8.4.31`, so a blind Next.js major bump does not prove closure. |
| Diff whitespace | `git diff --check` | `PASS` | Passed after formatting the new test. |

## Generated Artifact Cleanup Plan

Before staging or opening a PR, remove incidental generated outputs unless an
artifact is intentionally frozen with context:

- `build/`
- `dist/`
- `src/qa_z.egg-info/`
- `.qa-z/`
- `benchmarks/results/`
- `benchmarks/results-ci/`
- `.pytest_cache/`
- `.mypy_cache/`
- `.ruff_cache/`
- `examples/**/node_modules/`
- `examples/**/.next/`
- `examples/**/coverage/`
- `examples/**/.qa-z/`
- `examples/**/qa/contracts/`
- incidental `examples/**/package-lock.json`

## External Blockers

- Human release-owner approval for release execution.
- Human release-owner approval for package upload.
- TestPyPI/PyPI credentials and selected registry policy.
- Remote CI and public raw proof are recorded for current exact candidate SHA
  `865efa12fccc1976d6d9bf2cddec89bca6610f67` in
  `docs/reports/v0.10.0-beta-exact-sha-proof.md`; refresh this proof if
  `origin/main` moves before release-owner execution review.
- GitHub Release creation approval.
- Tag creation approval.
- Proof-branch or main push approval.
- Repository settings approval for topics and social preview.
- Static docs-site tooling/domain decision for hosted demo publication.
- Next.js dependency advisory decision; previous npm install reported
  `2` moderate advisories from `GHSA-qx2v-qp2m-jg93` / `CVE-2026-41305`
  because Next.js currently pins `postcss@8.4.31` while the PostCSS patched
  version is `8.5.10`. This report does not auto-fix dependencies.
  It does not run `npm audit fix --force`, and does not downgrade or override Next.js.
  The release-owner decision packet is
  `docs/reports/v0.10.0-beta-nextjs-advisory-decision.md`.
  It also does not add dependency overrides.

## Go/No-Go Recommendation

Recommendation: `NO-GO` for v0.10.0-beta release execution today.

Recommended next step: finish this local readiness evidence packet, keep
`registry_upload_executed=false`, and ask the release owner to decide whether
the next PR should be a non-executing version-policy PR, a proof-branch CI
packet, or a registry rehearsal in a credentialed release environment.
