# QA-Z v0.10.0-beta TestPyPI Rehearsal Approval Packet

Packet date: 2026-05-20
Source basis: clean `origin/main` at `53e9eff4eedc359646bf2ea6255a291997f7572b`
Decision type: release-owner selected-next-path approval packet, not release
execution.

Selected next path: TestPyPI rehearsal.

`v0.10.0-beta` is not released. Release execution remains `NO-GO`. This packet
records TestPyPI rehearsal as the selected next review path after GitHub Action
adoption troubleshooting closed. It does not run TestPyPI upload, does not
authorize package publish, and does not change the current package metadata.

## Purpose

The purpose of this packet is to move the release-owner decision from an open
package publish path matrix to a specific next review path: TestPyPI rehearsal.
The decision is intentionally non-executing. It gives the next packet a clear
target while keeping actual registry upload blocked until execution approval,
credentials, final SHA proof, rollback/yank policy, and generated-artifact
cleanup are present.

This approval does not authorize upload yet.

The TestPyPI rehearsal execution packet is
`docs/reports/v0.10.0-beta-testpypi-rehearsal-execution-packet.md`. It is the
next review surface for TestPyPI rehearsal, but it does not upload and release
execution remains `NO-GO`.

The final TestPyPI rehearsal GO/NO-GO packet is
`docs/reports/v0.10.0-beta-testpypi-rehearsal-go-no-go.md`. It records a
no-upload `NO_GO_MISSING_APPROVAL` decision until release-owner approval and
package-publish approval exist.

## Current State

- `v0.10.0-beta` is not released.
- Current public alpha remains `v0.9.9-alpha`.
- Current package metadata remains `0.9.8a0`.
- Current active install remains GitHub source/tag install until package publish
  is approved, executed, and proven.
- Tool-equipped no-upload tool smoke passed in
  `docs/reports/v0.10.0-beta-tool-smoke-execution.md`.
- The no-upload smoke is not TestPyPI publish proof.
- `registry_upload_executed=false`.
- Current release execution remains `NO-GO`.
- Registry credentials are not loaded or recorded here.
- Credential presence is not approval.

## Selected Next Path

Selected next path: TestPyPI rehearsal.

This means the next release-readiness packet may prepare a TestPyPI rehearsal
execution plan and proof checklist. It does not mean a package may be uploaded.
It does not choose PyPI publish, GitHub prerelease, tag creation, deployment, or
version metadata execution.

The selected rehearsal path should keep the package publish boundary at
`registry_upload_executed=false` until a later execution packet explicitly
records all approval and proof fields.

## What This Approval Does Not Authorize

This approval does not authorize:

- publish to TestPyPI;
- publish to PyPI;
- run `twine upload`;
- create a tag;
- create a GitHub Release;
- deploy;
- change `pyproject.toml`;
- bump version metadata;
- load registry credentials;
- mutate GitHub settings;
- post bot comments;
- call live model APIs;
- commit generated artifacts;
- claim `pipx install qa-z` is live;
- claim `uv tool install qa-z` is live.

## Required Before Actual TestPyPI Upload

Actual TestPyPI upload remains blocked until all of these exist in a later
execution packet:

- release-owner execution approval;
- `RELEASE_EXECUTION_APPROVED=true`;
- `PACKAGE_PUBLISH_ALLOWED=true`;
- exact release SHA proof generated after release-candidate SHA freeze;
- remote CI proof for the exact release SHA;
- Public Raw Hygiene proof for the exact release SHA;
- registry credential boundary with no secrets printed;
- TestPyPI target registry and package metadata version decision;
- rollback/yank policy check at execution time;
- Next.js/PostCSS advisory option/proof;
- generated-artifact cleanup proof;
- final execution packet.

`PACKAGE_PUBLISH_ALLOWED=true` is required before upload, but it is not enough
by itself. Credential presence is not approval, and local package smoke is not
publish proof.

## Required Evidence

A future TestPyPI rehearsal or upload packet must include:

- clean worktree status;
- exact source SHA and branch;
- current package metadata proof;
- tool-smoke proof from `docs/reports/v0.10.0-beta-tool-smoke-execution.md` or a
  fresh equivalent when artifacts change;
- `registry_upload_executed=false` for rehearsal-only packets;
- explicit PASS/FAIL/NOT RUN statuses for `twine`, `pipx`, and `uvx` checks;
- generated artifact cleanup status for `build/`, `dist/`,
  `src/qa_z.egg-info/`, `.qa-z/`, and benchmark result directories;
- release-owner approval fields for any upload packet;
- TestPyPI package URL/version proof only after an approved upload has occurred;
- explicit non-actions for any release-adjacent action not approved.

## Blocked Commands

These examples are blocked commands, not validation commands:

```bash
python -m twine upload --repository testpypi dist/*
python -m twine upload dist/*
git tag ...
gh release create ...
deploy commands
```

Blocked actions also include `uv publish`, npm registry publish, GitHub Packages
publish, TestPyPI package publish, PyPI package publish, tag creation, GitHub
Release creation, deployment, GitHub settings mutation, bot comments, live model
calls, changing `pyproject.toml`, bumping version metadata, and loading registry
credentials for this approval-only packet.

## Remaining Blockers

| Blocker | Status | Required next proof |
| --- | --- | --- |
| Release-owner execution approval | `BLOCKED` | A later execution packet must record owner approval and stop authority. |
| Package publish authorization | `BLOCKED` | `PACKAGE_PUBLISH_ALLOWED=true` is required before any registry upload. |
| Registry credentials | `BLOCKED` | Confirm out of band only after approval; never print credentials. |
| Version execution decision | `BLOCKED` | Keep `0.9.8a0` unless a separate approved version metadata PR changes it. |
| Exact release SHA proof | `BLOCKED` | Generate after release-candidate SHA freeze. |
| Rollback/yank policy check | `BLOCKED` | Recheck official TestPyPI/PyPI policy at execution time. |
| Advisory option/proof | `BLOCKED` | Release owner must choose and prove defer, exception, upstream wait, replacement/removal, or reviewed dependency change. |
| Generated artifact cleanup | `BLOCKED` | Confirm generated outputs are absent or intentionally untracked after any rehearsal. |
| Final execution packet | `BLOCKED` | Required before upload, tag, release, deploy, or version execution. |

## Explicit Non-actions

This packet did not:

- release `v0.10.0-beta`;
- publish to TestPyPI;
- publish to PyPI;
- run `twine upload`;
- run `uv publish`;
- run `npm publish`;
- create a tag;
- create a GitHub Release;
- deploy;
- change `pyproject.toml`;
- bump version metadata;
- load registry credentials;
- mutate GitHub settings;
- post bot comments;
- call live model APIs;
- commit generated artifacts;
- claim `pipx install qa-z` is live;
- claim `uv tool install qa-z` is live;
- authorize actual upload.

The next safe packet may prepare TestPyPI rehearsal execution details, but actual
upload remains blocked until release-owner execution approval, credentials,
final SHA proof, rollback/yank policy, advisory proof, and a final execution
packet exist.

Post-approval status update: a later TestPyPI-only upload proof is recorded in
`docs/reports/v0.10.0-beta-testpypi-rehearsal-upload-proof.md`. PyPI upload did
not occur. No tag, GitHub Release, deploy, or version bump occurred, and
`pipx install qa-z` remains not live for production PyPI.
