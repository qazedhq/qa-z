# QA-Z v0.18 Production PyPI GO/NO-GO

Date: 2026-05-27

Decision: `NO_GO_MISSING_APPROVAL`

Secondary decision: `NO_GO_MISSING_CREDENTIALS`

Release version: `0.10.0b0`

Final release SHA frozen: no. Production upload is blocked before release-SHA
freeze because the required approval flags are absent.

Local readiness source SHA checked:
`202e4b0c84439578694997db63492546ccb5e27e`

`origin/main` observed before the branch:
`ec65c90cdbd438ddc9210be412af98aa6a8be269`

## Decision Codes

| Code | Status | Evidence |
| --- | --- | --- |
| `GO_FOR_PRODUCTION_PYPI_UPLOAD` | not selected | Approval and credential gates are not satisfied. |
| `NO_GO_MISSING_APPROVAL` | selected | Required approval environment flags are missing. |
| `NO_GO_MISSING_CREDENTIALS` | secondary blocker | No PyPI API token or Trusted Publishing proof is present. |
| `NO_GO_STALE_SHA` | not selected | Upload stopped before release-SHA freeze; the checked source SHA is recorded above. |
| `NO_GO_BUILD_FAILED` | not selected | Local build passed. |
| `NO_GO_TWINE_CHECK_FAILED` | not selected | `twine check` passed after updating the local checker dependency. |
| `NO_GO_INSTALL_SMOKE_FAILED` | not selected | Installed-package smoke passed for wheel and sdist. |
| `NO_GO_POLICY_BLOCKER` | not selected | Policy blocker is the missing approval gate, recorded separately. |
| `NO_GO_EXTERNAL_SERVICE` | not selected | PyPI was reachable enough to confirm no package from the simple index. |

## Required Approval Flags

All of these must be present before any production PyPI upload command can run.

| Flag | Required value | Observed |
| --- | --- | --- |
| `PRODUCTION_PYPI_RELEASE_APPROVED` | `true` | missing |
| `PYPI_UPLOAD_ALLOWED` | `true` | missing |
| `PACKAGE_PUBLISH_ALLOWED` | `true` | missing |
| `TARGET_REGISTRY` | `PyPI` | missing |
| `RELEASE_VERSION` | `0.10.0b0` | missing |

Because the approval gate failed, no upload command was eligible to run.

## Credential Boundary

| Credential path | Observed | Release effect |
| --- | --- | --- |
| PyPI Trusted Publishing | no documented configured proof in this branch | blocked |
| `TWINE_USERNAME` / `TWINE_PASSWORD` | missing | blocked |
| `TWINE_API_TOKEN` | missing | blocked |
| `PYPI_API_TOKEN` | missing | blocked |
| `UV_PUBLISH_TOKEN` | missing | blocked |

Credential presence would not be sufficient on its own. The approval flags above
must also be present, and token values must never be printed or committed.

## Production PyPI State

- `python -m pip index versions qa-z`: `ERROR: No matching distribution found for qa-z`.
- `https://pypi.org/simple/qa-z/`: HTTP 404.
- Production PyPI package proof for `qa-z==0.10.0b0` does not exist in this
  branch.
- Historical TestPyPI proof remains separate at
  `https://test.pypi.org/project/qa-z/0.9.8a0/`.

## Safe Local Readiness Checks

These checks ran without any registry upload.

| Check | Result |
| --- | --- |
| `python -m build --sdist --wheel` | passed |
| `python -m twine check dist/qa_z-0.10.0b0-py3-none-any.whl dist/qa_z-0.10.0b0.tar.gz` | passed |
| `python scripts/installed_package_smoke.py --json` | passed |

The first `twine check` attempt used `twine==6.2.0` with `packaging==24.1` and
rejected modern Core Metadata 2.4 license fields. Updating only the local
checker dependency to `packaging==26.2` allowed the exact built artifacts to
validate. The package metadata and version were not changed.

## Built Artifacts

Generated locally, not committed:

| Artifact | Size bytes | SHA-256 |
| --- | ---: | --- |
| `dist/qa_z-0.10.0b0-py3-none-any.whl` | 389786 | `303148adc5287c74afb53c33c708531b79da3635a9667a14b039ad6f98e054a0` |
| `dist/qa_z-0.10.0b0.tar.gz` | 582485 | `47d76fbc193e60e0d6539832ac552a3dfd430d0f4bc98b75ad6c4d7cb0791e94` |

## Installed-Package Smoke Result

`python scripts/installed_package_smoke.py --json` returned
`"summary": "installed package smoke passed"` and
`"registry_upload_executed": false`.

Both wheel and sdist fresh virtual environments passed:

- install artifact
- `qa-z --help`
- `python -m qa_z --help`
- `qa-z demo auth-bug --json`
- `qa-z doctor --json`
- `qa-z guard --from-run latest --adapter codex`
- `qa-z repair-prompt --from-run latest --adapter codex`
- apply the deterministic auth-bug fixed file
- `qa-z verify --from-run latest --json` with improved verification

## README Transition Status

README transition status: unchanged.

The README must keep the GitHub source install path until production PyPI proof
exists. This packet does not authorize a live `pipx install qa-z`,
`uv tool install qa-z`, or `python -m pip install qa-z==0.10.0b0` README claim.

## Rollback / Yank Stance

No rollback or yank action is available because no production PyPI upload
occurred.

If a future approved upload succeeds, the release owner must decide the PyPI
yank stance before publishing and record whether the response to a bad artifact
is yank, replacement through a new version, or a separate advisory. QA-Z should
not assume a local undo command exists for a registry upload.

## Explicit Non-Actions

- `production_pypi_upload_executed=false`
- `registry_upload_executed=false` for production PyPI
- No `twine upload` command ran.
- No TestPyPI upload ran.
- No tag was created.
- No GitHub Release was created.
- No deploy occurred.
- No version bump occurred.
- No README live PyPI install claim was added.
- No credentials were printed or committed.
- Generated `dist/`, `build/`, `src/qa_z.egg-info/`, `.qa-z/`, caches, and temp
  virtual environments are cleanup-only artifacts and must not be committed.

## Remaining Gate

The next production PyPI attempt requires a new release-owner execution packet
with all approval flags present, credential or Trusted Publishing proof,
fresh final SHA proof, build and `twine check` proof for exact artifacts,
production upload execution, post-upload install smoke, README install
transition, and generated-artifact cleanup proof.

## v0.19 Governance Successor

The next governance packet is
`docs/reports/v0.19-production-pypi-governance.md`.

No upload occurred in v0.19. The v0.19 packet keeps production PyPI blocked,
recommends PyPI Trusted Publishing via GitHub Actions as the default production
path, keeps manual PyPI API token use as explicit fallback only, and moves
rollback/yank handling into
`docs/reports/v0.19-pypi-rollback-yank-playbook.md`.
