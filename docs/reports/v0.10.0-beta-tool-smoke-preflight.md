# QA-Z v0.10.0-beta Tool-smoke Preflight Packet

Packet date: 2026-05-17
Source basis: clean `origin/main` worktree at
`bc1cef8b2a38a1099f92bad52a6db11bf34640f2`.
Decision type: tool-equipped package-smoke preflight, not release execution.

Decision: `BLOCKED_TOOL_MISSING`.

`v0.10.0-beta` is not released. Release execution remains `NO-GO`. This packet
records whether the current clean environment is eligible to run no-upload
package smoke with `twine`, `pipx`, and `uvx`. Missing tools are not `PASS`.

## Purpose

This packet reduces the release-smoke blocker only by checking environment
eligibility. It separates preflight evidence from actual package-smoke evidence
so a missing local tool cannot be mistaken for a passed smoke.

Status vocabulary used by this packet:

- `READY_FOR_NO_UPLOAD_SMOKE`
- `BLOCKED_TOOL_MISSING`
- `BLOCKED_CREDENTIAL_PRESENT`
- `BLOCKED_DIRTY_WORKTREE`
- `NOT RUN`
- `PASS`
- `FAIL`

## Current No-release State

- Current release-owner decision remains `No release yet`.
- `v0.10.0-beta` is not released.
- Current public alpha remains `v0.9.9-alpha`.
- Current package metadata remains `0.9.8a0`.
- Release execution remains `NO-GO`.
- Tool-equipped `twine`/`pipx`/`uvx` smoke remains `NOT RUN`.
- `registry_upload_executed=false`.
- Credentials are not approval.

## Worktree Preflight

| Check | Result | Evidence |
| --- | --- | --- |
| Worktree path | `PASS` | `F:\JustTyping\.worktrees\v010-beta-tool-smoke-preflight` |
| Branch | `PASS` | `codex/v010-beta-tool-smoke-preflight` |
| `origin/main` SHA | `PASS` | `bc1cef8b2a38a1099f92bad52a6db11bf34640f2` |
| `HEAD` SHA | `PASS` | `bc1cef8b2a38a1099f92bad52a6db11bf34640f2` |
| `git status --short -uall` | `PASS` | clean output |

Worktree status: `PASS`. If this worktree becomes dirty before a future smoke
run, the smoke decision must change to `BLOCKED_DIRTY_WORKTREE`.

## Tool Availability

| Tool | Command | Status | Evidence |
| --- | --- | --- | --- |
| `twine` | `python -m twine --version` | `BLOCKED_TOOL_MISSING` | Python reported `No module named twine`. |
| `pipx` | `pipx --version` | `BLOCKED_TOOL_MISSING` | PowerShell reported `pipx` was not recognized. |
| `uvx` | `uvx --version` | `BLOCKED_TOOL_MISSING` | PowerShell reported `uvx` was not recognized. |

No tool install was performed. Because at least one required tool is missing,
and in this environment all three required tools are missing, this packet does
not authorize build, artifact smoke, package-smoke rehearsal, upload, tag,
GitHub Release, package publish, deploy, or version bump.

## Credential Boundary

Credential checks recorded only indicator names or absence markers. No
credential values were printed.
No credential values were printed.

| Check | Status | Evidence |
| --- | --- | --- |
| Package-publish environment variables | `PASS` | `NO_PACKAGE_PUBLISH_CREDENTIAL_ENV_FOUND` |
| User `.pypirc` | `PASS` | `PYPIRC_ABSENT` |
| Credential values printed | `PASS` | No values were printed. |

If a future preflight finds `TWINE_USERNAME`, `TWINE_PASSWORD`,
`TWINE_API_TOKEN`, `UV_PUBLISH_TOKEN`, `PYPI_TOKEN`, npm auth token indicators,
or `.pypirc`, the smoke decision must become `BLOCKED_CREDENTIAL_PRESENT` and
build/smoke must stop before any package command runs.

## Smoke Execution Decision

Decision: `BLOCKED_TOOL_MISSING`.

No-upload smoke did not run. Build did not run. Artifact smoke did not run.
`scripts/package_smoke_rehearsal.py` did not run.

The environment would become `READY_FOR_NO_UPLOAD_SMOKE` only if all of these
conditions are true at the same time:

- the worktree is clean;
- `twine`, `pipx`, and `uvx` are available and return versions;
- package-publish credentials are absent;
- release-owner boundaries still prohibit upload, tag, release, publish,
  deploy, and version bump.

## Smoke Results, if run

Smoke results: `NOT RUN`.

Expected no-upload smoke fields remain unavailable in this environment:

| Field | Current status |
| --- | --- |
| `twine_check` | `NOT RUN` |
| `pipx_wheel_help` | `NOT RUN` |
| `uvx_wheel_help` | `NOT RUN` |
| `registry_upload_executed` | `false` |

## What This Does Not Prove

This packet does not prove package smoke passed. It does not prove TestPyPI,
PyPI, npm, GitHub Packages, tag, GitHub Release, deploy, rollback, yank, final
SHA, advisory closure, or release-owner approval.

Preflight passing in a future environment would prove only that no-upload smoke
is eligible to run there. It would still not release `v0.10.0-beta`.

## Explicit Non-actions

This packet did not:

- release `v0.10.0-beta`;
- change `pyproject.toml`;
- bump version metadata;
- install `twine`, `pipx`, `uvx`, or other release tools;
- load registry credentials;
- print registry credential values;
- create a tag;
- create a GitHub Release;
- publish a package;
- run `twine upload`;
- run `uv publish`;
- run `npm publish`;
- deploy;
- mutate GitHub settings;
- post bot comments;
- call live model APIs;
- run build;
- run artifact smoke;
- run package-smoke rehearsal;
- claim missing tools are `PASS`.

No `twine upload` command was run. No tag command was run. No GitHub Release was
created. No package publish occurred. No deploy occurred. No version bump
occurred.

## Remaining Blockers

| Blocker | Status | Next safe local step |
| --- | --- | --- |
| Tool-equipped `twine`/`pipx`/`uvx` smoke | `BLOCKED_TOOL_MISSING` | Re-run this preflight in an environment where the tools are already provisioned; do not install them as part of this packet. |
| Release-owner approval | `BLOCKED` | Keep `No release yet` until the owner records approval and selected path. |
| Registry credentials | `BLOCKED` | Confirm out of band only after approval; credential presence is not approval. |
| Advisory option/proof | `BLOCKED` | Release owner must choose and prove the Next.js/PostCSS advisory path. |
| Version execution decision | `BLOCKED` | Release owner must choose whether metadata stays `0.9.8a0` or a separate version PR is required. |
| Final release-execution-time SHA proof | `BLOCKED` | Generate only after release-candidate SHA freeze and before approved execution. |
| Rollback/yank proof | `BLOCKED` | Produce actual proof only after an approved release path exists. |

Until those blockers are resolved with fresh evidence, release execution remains
`NO-GO`.
