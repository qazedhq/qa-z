# v0.30 - Public Beta Stabilization

Date: 2026-05-28

Status: local stabilization slice complete; external release remains blocked.

## Goal

Tie the v0.20-v0.30 roadmap to current local evidence, CLI commands, and release
boundaries before any public beta claim.

## Roadmap Evidence Matrix

| Milestone | Current evidence |
| --- | --- |
| v0.20 | `scripts/production_pypi_publish_gate.py` records production PyPI GO/NO-GO without upload. |
| v0.21 | `qa-z doctor` clean-path next actions point to demo, scorecard, and guard. |
| v0.22 | `qa-z init --profile auto --dry-run` prints Recommended profile, Confidence, Evidence, Activated checks, and Next. |
| v0.23 | `qa-z summary --from-run latest` remains the local evidence navigator for verdicts, risks, artifact paths, repair, and verify next commands. |
| v0.24 | `qa-z repair-session` and `qa-z verify --from-run latest` preserve the repair -> verify loop. |
| v0.25 | `qa-z github-summary` and the composite GitHub Action keep PR gate diagnostics and next actions local-first and artifact-backed. |
| v0.26 | `qa-z repair-prompt --adapter human` joins Codex, Claude, Cursor, aider, OpenHands, and generic handoffs. |
| v0.27 | `qa-z scorecard` reports local readiness dimensions and next actions. |
| v0.28 | `qa-z policy validate` and `qa-z guard --policy strict` provide local policy-pack validation. |
| v0.29 | `qa-z baseline create`, `qa-z waiver add`, and `qa-z governance report` provide local governance artifacts. |
| v0.30 | `tests/test_public_beta_stabilization.py` audits the CLI surface and this evidence report. |

## Stabilization Checks

The v0.30 local audit covers:

- CLI command availability for doctor, init, summary, repair-session,
  github-summary, repair-prompt, scorecard, policy, baseline, waiver, and
  governance.
- Current-truth report coverage for every v0.20-v0.30 roadmap milestone.
- Public beta boundary text for package publishing, deployment, and release
  non-actions.

## Release Boundary

Production PyPI remains blocked until release-owner approval flags, Trusted
Publishing or credential proof, final SHA proof, refreshed build and smoke
proof, production upload proof, and README install transition proof exist.

## Explicit Non-Actions

- No PyPI upload.
- No TestPyPI upload.
- No `twine upload`.
- No tag.
- No GitHub Release.
- No deploy.
- No README live PyPI install claim.
- No target-repository repair or external executor call.
