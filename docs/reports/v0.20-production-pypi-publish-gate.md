# v0.20 - Production PyPI Publish-Execution Gate

Date: 2026-05-27

Status: execution gate blocked. This packet adds a deterministic gate script for
the production PyPI publish decision, but it does not execute an upload.

Decision: `NO_GO_MISSING_APPROVAL`

Secondary blocker: `NO_GO_MISSING_CREDENTIALS`

Gate script: `scripts/production_pypi_publish_gate.py`

## Gate Evidence

The gate was run against the latest merged main SHA after PR #97, PR #89, and
PR #90 were merged:

```bash
python scripts/production_pypi_publish_gate.py --final-sha 425476382830baf35678b7daf876f285c590ad46 --local-proof docs/reports/v0.18-production-pypi-go-no-go.md --json
```

The command returned exit code `1`, which is expected for a blocked production
publish gate.

Key payload fields:

- `kind`: `qa_z.production_pypi_publish_gate`
- `current_sha`: `425476382830baf35678b7daf876f285c590ad46`
- `production_pypi_upload_executed`: `false`
- `registry_upload_executed`: `false`

| Field | Value |
| --- | --- |
| `kind` | `qa_z.production_pypi_publish_gate` |
| `decision` | `NO_GO_MISSING_APPROVAL` |
| `current_sha` | `425476382830baf35678b7daf876f285c590ad46` |
| `final_sha` | `425476382830baf35678b7daf876f285c590ad46` |
| `package_version` | `0.10.0b0` |
| `credential_or_trust_proof` | `missing` |
| `local_proof_exists` | `true` |
| `upload_allowed` | `false` |
| `production_pypi_upload_executed` | `false` |
| `registry_upload_executed` | `false` |

Missing approval flags:

- `PRODUCTION_PYPI_RELEASE_APPROVED`
- `PYPI_UPLOAD_ALLOWED`
- `PACKAGE_PUBLISH_ALLOWED`
- `TARGET_REGISTRY`
- `RELEASE_VERSION`

No secret values are printed by the gate. The payload records
`secret_value_keys_reported=[]`.

## Required Gate Before GO

The `GO_FOR_PRODUCTION_PYPI_UPLOAD` decision remains unavailable until all of
these are true in a later owner-approved execution packet:

1. Release-owner approval flags are present with the exact required values.
2. PyPI Trusted Publishing proof or out-of-band credential proof is recorded
   without printing secret values.
3. The approved final SHA matches the current publish candidate.
4. The local proof path records refreshed build, `twine check`, and installed
   package smoke evidence.
5. Production PyPI upload and post-upload install smoke are executed only by
   the protected release path.

## Explicit Non-Actions

- No production PyPI upload occurred in v0.20.
- No TestPyPI upload occurred in v0.20.
- No `twine upload` command ran.
- No `uv publish` command ran.
- No tag was created.
- No GitHub Release was created.
- No deploy occurred.
- No credential values were printed, loaded into docs, or committed.
- No README live PyPI install claim was added.
- No generated artifacts were committed.

## Current Install Boundary

The current active install path remains the GitHub source/tag install path.
`pipx install qa-z` and `uv tool install qa-z` stay future-only until production
PyPI proof and install smoke exist.

## Next Safe Slice

If production PyPI approval remains absent, continue with v0.21 first-run
onboarding or another local product slice. If a release owner later supplies the
required approval and trust/credential proof, rerun this gate and then execute a
separate protected upload packet.
