# QA-Z v0.10.0-beta TestPyPI Rehearsal Execution Packet

Packet date: 2026-05-21
Packet type: pre-execution proof and stop-rule packet, not registry execution.
Source ref checked: `origin/main`.
Source SHA checked: `0c4288d98f8826e891ab8cd46fd5157000cadff0`.

This packet does not upload. It prepares the TestPyPI rehearsal execution
boundary after `docs/reports/v0.10.0-beta-testpypi-rehearsal-approval.md`
selected TestPyPI rehearsal as the next package-publish review path.

Release execution remains `NO-GO`. `registry_upload_executed=false`.
`twine upload` remains blocked. The stop rule is simple: upload requires
explicit final approval before any registry command can run.

Follow-up final TestPyPI rehearsal GO/NO-GO packet:
`docs/reports/v0.10.0-beta-testpypi-rehearsal-go-no-go.md`. It does not upload,
records `NO_GO_MISSING_APPROVAL`, and keeps release execution `NO-GO`.

## Purpose

The purpose of this packet is to give the release owner a final pre-upload
review surface for a future TestPyPI rehearsal. It fixes the exact current
`origin/main` SHA, records the currently available CI and Public Raw Hygiene
proof, names the expected build artifacts, and defines the stop rule before any
package registry upload can run.

This packet does not publish to TestPyPI or PyPI. It does not run
`twine upload`. It does not create a tag, create a GitHub Release, deploy, bump
`pyproject.toml`, load credentials, or claim `pipx install qa-z` is live.

Because this packet is committed through a normal PR, its SHA proof becomes
historical after merge. Final release-execution-time proof must still be
generated after release-candidate SHA freeze and immediately before any
approved execution.

## Current State

Pre-edit clean-worktree proof collected in a clean worktree from current
`origin/main`:

| Field | Status | Evidence |
| --- | --- | --- |
| `git status --short -uall` | `PASS` | Empty output before this packet and test were created. |
| `git rev-parse origin/main` | `PASS` | `0c4288d98f8826e891ab8cd46fd5157000cadff0` |
| `git log -1 --oneline origin/main` | `PASS` | `0c4288d Merge pull request #81 from qazedhq/codex/v010-testpypi-rehearsal-approval` |
| `pyproject.toml` package name | `PASS` | `qa-z` |
| `pyproject.toml` package version | `PASS` | `0.9.8a0` |
| `v0.10.0-beta*` tag | `PASS` | No matching remote tag returned by `git ls-remote --tags origin 'v0.10.0-beta*'`. |
| `v0.10.0-beta` GitHub Release | `PASS` | `gh release list` showed only `v0.9.9-alpha` and `v0.9.8-alpha`. |
| PyPI JSON endpoint | `PASS` | `https://pypi.org/pypi/qa-z/json` returned HTTP 404. |
| TestPyPI JSON endpoint | `PASS` | `https://test.pypi.org/pypi/qa-z/json` returned HTTP 404. |
| Main CI proof | `PASS` | `CI` run `26227786956` on `main` at `0c4288d98f8826e891ab8cd46fd5157000cadff0` succeeded. |
| Main Public Raw Hygiene proof | `PASS` | `Public Raw Hygiene` run `26227786637` on `main` at `0c4288d98f8826e891ab8cd46fd5157000cadff0` succeeded. |

Package publish proof does not exist yet. No TestPyPI package URL/version proof
exists yet, and no PyPI package URL/version proof exists yet.
`pipx install qa-z` is not live, and `uv tool install qa-z` is not live.

## Required Approval Fields

These fields are required before any TestPyPI upload packet can run:

| Field | Current state | Required before upload |
| --- | --- | --- |
| `RELEASE_EXECUTION_APPROVED` | `NOT APPROVED` | Must be `true` in a later final execution packet. |
| `PACKAGE_PUBLISH_ALLOWED` | `NOT APPROVED` | Must be `true` before any registry upload. |
| release owner | `NOT APPROVED` | Named human release owner and stop authority. |
| selected path | `TestPyPI rehearsal` | Must remain selected and scoped to TestPyPI. |
| target package metadata version | `BLOCKED` | Current source remains `0.9.8a0`; any change requires a separate approved metadata PR. |
| target registry | `BLOCKED` | TestPyPI must be explicitly selected in the final packet. |
| exact release SHA | `BLOCKED` | Must be frozen after all source-changing PRs are done. |
| remote CI proof link | `PASS` for current `origin/main`, not final execution proof | Must be fresh for the frozen SHA. |
| Public Raw Hygiene proof link | `PASS` for current `origin/main`, not final execution proof | Must be fresh for the frozen SHA. |
| package smoke proof | historical local no-upload `PASS` | Refresh if artifacts or release-candidate SHA change. |
| advisory proof | `BLOCKED` | Release owner must choose and prove the advisory path. |
| rollback/yank policy check | `BLOCKED` | Recheck official PyPI/TestPyPI policy at execution time. |
| credential confirmation | `BLOCKED` | Confirm out of band only after approval; credentials must not be printed. |

Credential presence is not approval.

## Exact SHA Proof Requirements

The current checked `origin/main` SHA is
`0c4288d98f8826e891ab8cd46fd5157000cadff0`. It has successful `CI` and
`Public Raw Hygiene` runs, but this PR will move the branch when merged.

A later upload packet must regenerate final release-execution-time proof after
the release-candidate SHA is frozen. The final proof must include:

- exact source SHA and source ref;
- fresh `CI` proof for that SHA;
- fresh `Public Raw Hygiene` proof for that SHA;
- exact-SHA public raw checks for release-critical files;
- package metadata proof;
- package smoke and `twine check` proof;
- advisory proof;
- credential boundary proof without secrets;
- rollback/yank policy check;
- generated-artifact cleanup proof;
- final freshness check immediately before execution.

If `origin/main` or the selected release-candidate ref moves after this packet,
this packet is historical evidence only.

## Artifact Build Plan

The current metadata implies these expected artifact names when a future local
build runs from the selected source:

| Artifact | Source of expectation |
| --- | --- |
| `qa_z-0.9.8a0-py3-none-any.whl` | current `pyproject.toml` version and previous no-upload smoke |
| `qa_z-0.9.8a0.tar.gz` | current `pyproject.toml` version and previous no-upload smoke |

The previous tool-equipped no-upload smoke packet,
`docs/reports/v0.10.0-beta-tool-smoke-execution.md`, built
`qa_z-0.9.8a0-py3-none-any.whl` and `qa_z-0.9.8a0.tar.gz` to a temporary
directory and did not commit generated artifacts.

For final rehearsal, build only into a temporary or cleanup-tracked output
directory:

```bash
python -m build --sdist --wheel --outdir <temp-dist>
```

After the build, record the exact artifact names and sizes, then prove `build/`,
`dist/`, `src/qa_z.egg-info/`, `.qa-z/`, benchmark outputs, and caches are
absent from the source commit or intentionally untracked and cleaned.

## Twine Check Plan

`twine check` is allowed only as a no-upload package validation step. It must
not be mixed with `twine upload`.

Final rehearsal should run:

```bash
python -m twine check <temp-dist>/qa_z-0.9.8a0-py3-none-any.whl <temp-dist>/qa_z-0.9.8a0.tar.gz
```

The earlier tool-smoke execution packet recorded `twine_check` as `PASS` with
`registry_upload_executed=false`. This packet does not rerun `twine check`;
local `python -m twine --version` was unavailable in this worktree. A later
execution packet must either provide an isolated tool environment and rerun
`twine check`, or mark the gate as `NOT RUN`/`BLOCKED` with the reason.

## Credential Boundary

Credentials must not be printed. This packet does not load registry credentials.

The final packet must confirm credential readiness out of band only after both
approval fields are present:

- `RELEASE_EXECUTION_APPROVED=true`;
- `PACKAGE_PUBLISH_ALLOWED=true`.

Do not print `.pypirc`, `TWINE_USERNAME`, `TWINE_PASSWORD`, `TWINE_API_TOKEN`,
`UV_PUBLISH_TOKEN`, PyPI tokens, TestPyPI tokens, npm tokens, or GitHub package
tokens. Do not use credential presence as approval.

## Rollback/Yank Execution-time Check

Rollback/yank policy remains `BLOCKED` until execution time. The existing policy
packet is `docs/reports/v0.10.0-beta-rollback-yank-policy.md`, but final upload
approval must recheck official policy at execution time.

Official PyPI docs checked during this packet:

- PyPI yanking docs: `https://docs.pypi.org/project-management/yanking/`
- PyPI storage/deletion docs:
  `https://docs.pypi.org/project-management/storage-limits/`

The current PyPI docs state that yanking is non-destructive, deletion is
different from yanking, and deletion is permanent without administrative
intervention. Therefore, a final TestPyPI packet must name the owner-approved
retention, yank, or delete stance before upload. Local cleanup is not rollback
proof for a registry artifact.

## Upload Stop Rule

Stop before upload unless every item below is true in a later final packet:

- `RELEASE_EXECUTION_APPROVED=true`;
- `PACKAGE_PUBLISH_ALLOWED=true`;
- exact release-candidate SHA is frozen;
- fresh CI proof exists for the frozen SHA;
- fresh Public Raw Hygiene proof exists for the frozen SHA;
- current package metadata and exact artifact names are recorded;
- `twine check` passed for the exact artifacts;
- TestPyPI credentials are confirmed without printing secrets;
- advisory proof is accepted by the release owner;
- rollback/yank policy is checked at execution time;
- generated-artifact cleanup proof exists;
- final freshness check confirms the selected ref still points to the frozen SHA.

If any item is missing, stop. Keep `registry_upload_executed=false`.

## Explicit Non-actions

This packet did not:

- upload to TestPyPI;
- upload to PyPI;
- run `twine upload`;
- run `uv publish`;
- run `npm publish`;
- create a tag;
- create a GitHub Release;
- deploy;
- change `pyproject.toml`;
- bump version metadata;
- load registry credentials;
- print credentials;
- mutate GitHub settings;
- post bot comments;
- call live model APIs;
- commit generated artifacts;
- claim `pipx install qa-z` is live;
- claim `uv tool install qa-z` is live;
- create package publish proof.

## Remaining Blockers

| Blocker | Status | Required next proof |
| --- | --- | --- |
| Release-owner execution approval | `BLOCKED` | Final packet must record owner approval and stop authority. |
| Package publish authorization | `BLOCKED` | `PACKAGE_PUBLISH_ALLOWED=true` required before upload. |
| TestPyPI credentials | `BLOCKED` | Confirm out of band only after approval; never print secrets. |
| Final SHA proof | `BLOCKED` | Regenerate after release-candidate SHA freeze. |
| Rollback/yank execution-time check | `BLOCKED` | Recheck official PyPI/TestPyPI policy immediately before upload. |
| Advisory proof | `BLOCKED` | Release owner must choose and prove the advisory path. |
| Generated-artifact cleanup proof | `BLOCKED` | Prove build outputs and caches are absent or intentionally untracked after rehearsal. |
| Final execution packet | `BLOCKED` | Required before any TestPyPI upload, tag, release, deploy, or version execution. |

Until those blockers are resolved, release execution remains `NO-GO`, package
publish proof does not exist yet, `twine upload` remains blocked, and
`registry_upload_executed=false`.
