# QA-Z v0.10.0-beta Tool-smoke Execution Packet

Packet date: 2026-05-17
Source basis: clean `origin/main` worktree at
`b63d1f43f6a194e2c9302238a7557760e5cc492b`.
Decision type: tool-equipped no-upload package-smoke execution, not release
execution.

Decision: `NO_UPLOAD_SMOKE_PASS`.

`v0.10.0-beta` is not released. Release execution remains `NO-GO`. This packet
records a local no-upload package-smoke rehearsal with `twine`, `pipx`, and
`uvx` available in an isolated tool environment. It does not approve package
upload, tag creation, GitHub Release creation, deployment, version bump, or
registry credential use.

## Source And Environment

| Check | Result | Evidence |
| --- | --- | --- |
| Worktree path | `PASS` | `F:\JustTyping\.worktrees\v010-beta-tool-smoke-execution` |
| Branch | `PASS` | `codex/v010-beta-tool-smoke-execution` |
| Source SHA | `PASS` | `b63d1f43f6a194e2c9302238a7557760e5cc492b` |
| `git status --short -uall` | `PASS` | clean output before smoke; only ignored build/egg-info outputs were produced by build tooling. |
| Package-publish credential env | `PASS` | no `TWINE_*`, `UV_PUBLISH_*`, `PYPI*`, `TESTPYPI*`, `NPM_TOKEN`, or `NODE_AUTH_TOKEN` variables were present. |
| User `.pypirc` | `PASS` | `PYPIRC_ABSENT`. |

Tool versions:

| Tool | Version proof |
| --- | --- |
| `twine` | `twine version 6.2.0` |
| `pipx` | `1.12.0` |
| `uvx` | `uvx 0.11.14` |

The tools were provided through an isolated temporary tool venv and temporary
`PIPX_HOME`, `PIPX_BIN_DIR`, and `UV_CACHE_DIR` values. No global tool install,
registry credential load, or upload command was performed.

## Built Artifacts

Artifacts were built to a temporary output directory, not to repository-tracked
source:

| Artifact | Bytes |
| --- | ---: |
| `qa_z-0.9.8a0-py3-none-any.whl` | `370796` |
| `qa_z-0.9.8a0.tar.gz` | `547313` |

Build command:

```bash
python -m build --sdist --wheel --outdir <temp-dist>
```

## Package-smoke Result

Command:

```bash
python scripts/package_smoke_rehearsal.py --wheel <temp-dist>/qa_z-0.9.8a0-py3-none-any.whl --sdist <temp-dist>/qa_z-0.9.8a0.tar.gz --json
```

Result:

```json
{
  "summary": "package smoke rehearsal passed",
  "exit_code": 0,
  "checks": [
    {"id": "twine_check", "status": "PASS", "exit_code": 0},
    {"id": "pipx_wheel_help", "status": "PASS", "exit_code": 0},
    {"id": "uvx_wheel_help", "status": "PASS", "exit_code": 0}
  ],
  "registry_upload_executed": false
}
```

The package-smoke blocker is now reduced to local no-upload proof for this
source SHA. This proof does not prove registry publish, package availability,
tag creation, GitHub Release creation, deployment, final SHA proof, advisory
closure, registry credentials, rollback/yank execution, or release-owner
approval.

## Gate Delta

| Gate | Previous status | Current status | Evidence |
| --- | --- | --- | --- |
| tool-equipped `twine`/`pipx`/`uvx` smoke | `NOT RUN` / `BLOCKED_TOOL_MISSING` | `PASS` | `scripts/package_smoke_rehearsal.py` returned exit code `0` with all three checks `PASS`. |
| registry upload | blocked | blocked | `registry_upload_executed=false`; no `twine upload`, `uv publish`, `npm publish`, or package registry command ran. |
| release execution | `NO-GO` | `NO-GO` | Human approval, final SHA proof, advisory decision/proof, registry credentials, version decision, and rollback/yank proof remain open. |

## Explicit Non-actions

This packet did not:

- release `v0.10.0-beta`;
- change `pyproject.toml`;
- bump version metadata;
- create a tag;
- create a GitHub Release;
- publish a package;
- run `twine upload`;
- run `uv publish`;
- run `npm publish`;
- deploy;
- load or print registry credentials;
- mutate GitHub settings;
- post bot comments;
- call live model APIs;
- commit generated artifacts;
- resolve the Next.js/PostCSS advisory;
- provide final release-execution-time SHA proof;
- approve rollback/yank policy.

## Remaining Blockers

| Blocker | Status | Next safe local step |
| --- | --- | --- |
| Release-owner approval and selected release path | `BLOCKED` | Owner must choose no release, GitHub prerelease, metadata-only PR, TestPyPI rehearsal, TestPyPI publish, PyPI publish, or another explicit path. |
| Next.js/PostCSS advisory option and proof | `BLOCKED` | Owner must choose and prove defer, exception, upstream wait, replacement/removal, or reviewed dependency change. |
| Registry credentials | `BLOCKED` | Confirm out of band only after approval; credential presence is not approval. |
| Version execution decision | `BLOCKED` | Keep `0.9.8a0` unless a separate approved version metadata PR changes it. |
| Final release-execution-time SHA proof | `BLOCKED` | Generate only after release-candidate SHA freeze and before any approved execution. |
| Rollback/yank proof | `BLOCKED` | Produce actual proof only after an approved release path exists. |

Until those blockers are resolved with fresh evidence, release execution remains
`NO-GO`.
