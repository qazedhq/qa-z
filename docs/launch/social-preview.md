# Social Preview

Final copy:

```text
QA-Z 🛡️
Make AI coding safe to merge.
Contracts. Checks. Repair prompts. Verification.
```

Use the checked-in launch preview assets as the source for GitHub's repository social preview:

- PNG upload target: [../assets/qa-z-social-preview.png](../assets/qa-z-social-preview.png)
- Editable source: [../assets/qa-z-social-preview.svg](../assets/qa-z-social-preview.svg)
- Size: `1280 x 640`

Exact GitHub UI steps:

1. Open `https://github.com/qazedhq/qa-z/settings`.
2. In **General**, scroll to **Social preview**.
3. Select **Upload image**.
4. Upload `docs/assets/qa-z-social-preview.png`.
5. Save the repository settings and re-open the public repository page to confirm the preview.

Status: this commit updates the repository asset plan only. It does not claim the GitHub UI social preview has been set.

Do not add fake screenshots, fake adoption numbers, or package-registry claims.
If the copy changes, update the SVG and regenerate the PNG from that source.
