# QA-Z v0.9.8-alpha

QA-Z `v0.9.8-alpha` is a Codex-first, model-agnostic QA control plane alpha for coding-agent workflows. The matching Python package version is `0.9.8a0`.

## Included

- Deterministic `init`, `plan`, `fast`, `deep`, `review`, and `repair-prompt` command surfaces remain the core contract.
- Fast and deep QA gates produce repairable evidence through local run artifacts, SARIF, GitHub summary output, and post-repair verification.
- Benchmark fixtures cover Python, TypeScript, mixed fast/deep handoff aggregation, executor bridge action context, executor-result ingest, and the live-free safety dry-run.
- The root repository gate uses a Python-only `qa-z.yaml`; TypeScript coverage remains in `qa-z.yaml.example`, the demo, and benchmark fixtures.

## Not Included

- QA-Z does not perform live repair or call Codex, Claude, or another agent automatically in this alpha.
- Deep QA remains deterministic Semgrep-backed execution, not an LLM-only judgment layer.
- Generated runtime artifacts under `.qa-z/**` and `benchmarks/results/**` stay local unless a release commit explicitly freezes evidence with context.

## Validation Snapshot

- `python -m ruff format --check .`: 1014 files already formatted
- `python -m ruff check .`: passed
- `python -m mypy src tests`: 498 source files
- `python -m pytest`: 1158 passed
- `python -m qa_z fast --selection smart --json`: passed
- `python -m qa_z deep --selection smart --json`: passed; non-blocking `scan_quality` warnings surfaced
- `python -m qa_z benchmark --json`: 54/54 fixtures, overall_rate 1.0
- worktree commit-plan `python scripts/worktree_commit_plan.py --include-ignored --json`: ready, with
  source/generated commit-split counts summarized under
  `evidence.worktree_commit_plan` when run through the local release gate
- `python -m build --sdist --wheel`: passed
- `python scripts/alpha_release_artifact_smoke.py --json`: passed
- `python scripts/alpha_release_gate.py --json`: passed as the one-shot local alpha release gate
- CLI smoke checks: 17 help surfaces passed

The local release PR body draft is in `docs/releases/v0.9.8-alpha-pr.md`.
The local GitHub release note body draft is in `docs/releases/v0.9.8-alpha-github-release.md`.
The local publish handoff is in `docs/releases/v0.9.8-alpha-publish-handoff.md`.
The local preflight helper is `scripts/alpha_release_preflight.py`.
The one-shot local alpha release gate is `scripts/alpha_release_gate.py`; use
`--strict-worktree-plan` on final source-staging audits when generated local
evidence or cross-cutting patch-add ownership should block publish readiness.
That strict mode passes `--fail-on-generated --fail-on-cross-cutting` to the
worktree commit-plan helper.
The intended GitHub target is `qazedhq/qa-z`. The configured `origin` remote
now points to `https://github.com/qazedhq/qa-z.git`, but the latest remote
preflight still returns `404 Not Found`, so push and tag work stay blocked
until the repository exists and becomes reachable.

Local preflight and gate evidence now surface `actual_origin_target`, optional
`remote_ref_count`, optional `remote_ref_head_count`, optional
`remote_ref_tag_count`, optional `remote_ref_kinds`, optional
`remote_ref_sample`, optional `release_path_state`, optional `publish_checklist`, and compact
`next_action_count` / `next_command_count` rollups. When a matching `origin`
exists but remote checks are intentionally deferred, that state is labeled
`ready_for_remote_checks`, with `release_path_state=local_only_remote_preflight`
capturing that the verdict is still a local-only handoff rather than a live
remote publish. When `origin` is still missing, the same contract records
`publish_strategy=bootstrap_origin` and a bootstrap checklist that points to the
exact `git remote add origin ...` and rerun command.

