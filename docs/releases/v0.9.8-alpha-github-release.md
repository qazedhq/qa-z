# QA-Z v0.9.8-alpha

QA-Z `v0.9.8-alpha` is the first public alpha release candidate for the
Codex-first, model-agnostic QA control plane in this repository. The matching
Python package version is `0.9.8a0`.

## Highlights

- Deterministic `fast` and `deep` QA gates with repairable local artifacts.
- Review packet and `repair-prompt` generation for failed fast checks and deep findings.
- Repair-session manifests, executor guides, post-repair verification, and publish summaries.
- GitHub Actions summary rendering plus SARIF output for deep findings.
- Benchmark corpus covering Python, TypeScript, mixed fast/deep handoff aggregation, executor bridge action context, executor-result ingest, and live-free executor dry-run policies.
- Self-inspection, backlog, selected-task, and autonomy planning artifacts for local operator handoff.
- Executor bridge packaging and executor-result ingest without live agent execution.
- Frozen pre-live executor safety package and local live-free safety dry-run diagnostics.

## Validation

- `python -m ruff format --check .`: passed, `1014 files already formatted`
- `python -m ruff check .`: passed, `All checks passed!`
- `python -m mypy src tests`: passed, `Success: no issues found in 498 source files`
- `python -m pytest`: passed, `1158 passed`
- `python -m qa_z fast --selection smart --json`: passed, 4 checks passed, pytest tail reported `1158 passed`
- `python -m qa_z deep --selection smart --json`: passed, Semgrep reported `0 findings` and `0 blocking`; non-blocking `scan_quality` warnings surfaced
- `python -m qa_z benchmark --json`: passed, `54/54 fixtures, overall_rate 1.0`
- `python -m build --sdist --wheel`: passed, built `qa_z-0.9.8a0.tar.gz` and `qa_z-0.9.8a0-py3-none-any.whl`
- `python scripts/alpha_release_artifact_smoke.py --json`: passed, wheel and sdist metadata install smoke
- `python scripts/alpha_release_gate.py --json`: passed as the one-shot local alpha release gate
- CLI smoke checks: passed for 17 help surfaces

## Generated Artifact Policy

Root `.qa-z/**`, `benchmarks/results/work/**`,
`benchmarks/results/summary.json`, and `benchmarks/results/report.md` stay local
by default. Commit those files only when a release intentionally freezes evidence
with surrounding context.

Fixture-local `.qa-z/**` evidence under `benchmarks/fixtures/**/repo/.qa-z/**`
can remain tracked when it is part of a benchmark fixture contract.

## Known Non-Goals

- No live Codex or Claude execution.
- No autonomous code editing.
- No remote orchestration, queues, schedulers, or daemons.
- No branch mutation, commits, pushes, or GitHub bot comments from QA-Z itself.
- No LLM-only judgment replacing deterministic pass/fail gates.

## Publish Blocker

This release note body is prepared locally because the configured `origin` remote now points to `https://github.com/qazedhq/qa-z.git`, but the latest remote preflight still returns `404 Not Found` for `qazedhq/qa-z`. Publish only after the intended repository target exists, the release baseline is pushed, and remote CI passes.

## Remote Publish Evidence

The local publish path now records `actual_origin_target`, optional
`remote_ref_count`, optional `remote_ref_head_count`, optional
`remote_ref_tag_count`, optional `remote_ref_kinds`, optional
`remote_ref_sample`, optional `release_path_state`, optional `publish_checklist`, and compact
`next_action_count` / `next_command_count` evidence so release operators can see
which remote alignment state produced the current verdict.

If `origin` is already aligned and the operator runs the local helper with
`--skip-remote`, the preflight contract marks that state as
`ready_for_remote_checks`, records
`release_path_state=local_only_remote_preflight`, and emits the deterministic
rerun command for the next remote validation pass.

If `origin` is still missing, the same local-only path records
`publish_strategy=bootstrap_origin` and keeps the bootstrap checklist attached
to the artifact so the release operator can add `origin` and rerun preflight
without reconstructing the command sequence from scratch.

