# Release QA-Z v0.9.8-alpha

## Scope Summary

QA-Z `v0.9.8-alpha` packages the current Codex-first, model-agnostic QA control
plane baseline for local coding-agent workflows.

This release includes deterministic fast and deep QA gates, review and
repair-prompt artifacts, repair sessions, post-repair verification, GitHub
summary rendering, a benchmark corpus, self-inspection and autonomy planning
artifacts, executor bridge packaging, executor-result ingest, and a live-free
executor safety dry-run.

The matching Python package version is `0.9.8a0`.

## Validation Evidence

- `python -m ruff format --check .`: passed, `1014 files already formatted`
- `python -m ruff check .`: passed, `All checks passed!`
- `python -m mypy src tests`: passed, `Success: no issues found in 498 source files`
- `python -m pytest`: passed, `1158 passed`
- `python -m qa_z fast --selection smart --json`: passed, 4 checks passed, pytest tail reported `1158 passed`
- `python -m qa_z deep --selection smart --json`: passed, Semgrep reported `0 findings` and `0 blocking`; non-blocking `scan_quality` warnings surfaced
- `python -m qa_z benchmark --json`: passed, `54/54 fixtures, overall_rate 1.0`
- `python -m build --sdist --wheel`: passed, built `qa_z-0.9.8a0.tar.gz` and `qa_z-0.9.8a0-py3-none-any.whl`
- `python scripts/alpha_release_artifact_smoke.py --json`: passed, wheel and sdist metadata install smoke
- `python scripts/alpha_release_gate.py --json`: passed as the one-shot local alpha release gate
- CLI smoke checks: passed for 17 help surfaces, from `python -m qa_z --help` through `python -m qa_z executor-result --help`

## Known Non-Goals

- No live Codex or Claude execution.
- No autonomous code editing.
- No remote orchestration, queues, schedulers, or daemons.
- No branch mutation, commits, pushes, or GitHub bot comments from QA-Z itself.
- No LLM-only judgment replacing deterministic pass/fail gates.

## Generated Artifact Policy

Root `.qa-z/**`, `benchmarks/results/work/**`,
`benchmarks/results/summary.json`, and `benchmarks/results/report.md` stay local
by default. Commit those files only when a release intentionally freezes evidence
with surrounding context.

Fixture-local `.qa-z/**` evidence under `benchmarks/fixtures/**/repo/.qa-z/**`
can remain tracked when it is part of a benchmark fixture contract.

## Benchmark Snapshot

The current benchmark snapshot is `54/54 fixtures, overall_rate 1.0`.

The benchmark corpus covers Python and TypeScript fast failures, mixed
fast/deep handoff aggregation, executor bridge action-context packaging,
executor-result ingest, and live-free executor-result dry-run policies.

## Publish Blocker

This PR body is prepared locally because the configured `origin` remote now
points to `https://github.com/qazedhq/qa-z.git`, but the latest remote
preflight still returns `404 Not Found` for `qazedhq/qa-z`. Push and PR
creation should start only after that repository target exists and is
reachable.

## Remote Publish Evidence

Local preflight and the one-shot alpha gate now surface canonical remote
alignment evidence for publish handoff: `actual_origin_target`, optional
`remote_ref_count`, optional `remote_ref_head_count`, optional
`remote_ref_tag_count`, optional `remote_ref_kinds`, optional
`remote_ref_sample`, optional `release_path_state`, optional `publish_checklist`, and compact
`next_action_count` / `next_command_count` rollups for the nested local
preflight result. Successful remote paths resolve to
`release_path_state=remote_direct_publish` or
`release_path_state=remote_release_pr`; local-only rehearsal stays labeled as
`release_path_state=local_only_remote_preflight`. Missing-origin rehearsal now
records `publish_strategy=bootstrap_origin` so the PR handoff keeps the origin
bootstrap step explicit before any remote publish happens.

When `origin` is configured, matches the intended repository target, and the
operator intentionally runs local checks with `--skip-remote`, the decision is
classified as `ready_for_remote_checks` so the next remote validation step is
explicit instead of implied.

